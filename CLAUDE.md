# ZZAIMY 캡스톤 — 작업 지침

영남이공대학교 행정문서 처리 플랫폼 「짜이미」. 국고사업 계획서 초안, 채용·입학 서류 검토,
규정 응답, 문서 판독을 한 파이프라인에서 다루고(축 A), 오픈웨이트 모델 4종을 교내 문서로
특화한다(축 B). 산출물은 70% 초안이며 최종 판단은 담당자가 한다.

## 먼저 읽을 것

1. `PROJECT_BRIEF.md` — 판단의 기준(v3, 우리 실정). 여기와 충돌하는 구현은 틀린 구현이다
2. `docs/HANDOFF.md` — 지금 어떤 구성으로 도는가, 남은 일, 이어받는 절차
3. `docs/dev-now.md` — 지금 하는 일·다음 일(화면에도 나온다)
4. `docs/context/working-notes.md` — 축적된 사용자 선호·작업 규율·주의사항
5. `docs/decisions/` — 결정과 근거(ADR). 새 결정은 여기에 번호로

Codex·Claude 공동 작업이면 `docs/collaboration/README.md`와 `codex.md`·`claude.md`를 먼저 본다.
설계 문서(`docs/architecture.md` 등)와 `research/`는 근거 참조용이며 현황은 위 문서가 우선한다.

## 상황 (2026-09-22)

- 기간: 2026-09-01 ~ 11-30 구축·논문, 12월 초 교내 발표회. 13주 계획은 `docs/capstone-plan.md`
- 장비: 운영 = ESXi VM(CPU, 웹·검색·OCR) / 서빙 = 젯슨 토르 02(대화·초안)·03(검토·판독·임베딩·리랭커),
  둘 다 Writer 27B NVFP4 / 학습 = DGX(.110, SSH dgx-01 포트 8022) 전용. 상세와 접속은 `docs/HANDOFF.md` §1
- 데이터: 실물 표본은 `data/samples/`(git 제외). 지금 문서함은 공개 국고문서 + 교내 공개 문서 194건,
  교내 실물 문서는 수령 대기
- 단계: 플랫폼 가동 중. 검색 학습본(Embed v2·Rerank v1) 운영 채택. Writer·Extract 학습은 실물 문서와
  베이스라인 측정 뒤
- 접속이 한꺼번에 안 되면 사용자 VPN 문제다 — 고장으로 단정하지 말고 기다렸다 재시도

## 절대 규칙

브리프 9장. 어기면 되돌리는 비용이 크거나 프로젝트가 실패한다.

1. 수치는 인출하고 생성하지 않는다. 생성된 모든 수치는 인출된 근거에 있어야 한다
2. 베이스라인 측정 전에 파인튜닝을 시작하지 않는다
3. PII 스크리닝은 인덱싱 이전에. 기준 문서(규정·공고)는 마스킹하지 않는다(ADR-0006)
4. 열람 등급 필터링은 검색 단계에
5. 예산 계산에 LLM을 쓰지 않는다
6. 문서 계열을 단일 인덱스로 통합하지 않는다
7. 파일럿 우선 — 계획서 20~30건 + 대응 결과보고서
8. 모델에 지식을 주입하지 않는다. 능력만 학습, 지식은 검색으로
9. 모델은 계획한 4종만. 문서에 없는 모델을 편의로 넣지 않는다 — 새 후보는 ADR 뒤 대결 수치로만 채택
10. 특정 사례에 맞춘 규칙(하드코딩)으로 성능을 올리지 않는다. 일반 메커니즘만
11. ④ 지출 문서 계열을 이번 범위에 넣지 않는다

## 저장소 규칙

- 데이터를 커밋하지 않는다 — 실제 문서·파싱 결과·실적 카드·비밀번호는 `data/`와 `.env.local`에만.
  `.gitignore`가 막지만 `git add` 전에 `git status`를 본다
- 결정은 ADR로 — `docs/decisions/0000-template.md`. 기존 기록은 고쳐 쓰지 않고 새 번호로, 옛 것은 `대체됨`
- 새 것을 넣으면 문서(HANDOFF·dev-now·해당 설계 문서)를 같이 고치고 기존 검증(테스트·스모크·PII·
  반입 자가 점검·서빙 점검·측정)을 다시 돌린다
- 화면·보고서의 수치는 기계 산출물에서만. 측정은 `.env.local`을 읽고 띄운다(안 읽으면 CPU 폴백으로 거짓 수치)
- `research/`는 Phase 0 조사 기록(참고)이라 고치지 않는다. `docs/business-plan.md`는 원안 사업계획서(참고)

## 구조

```
src/zzaimy/
  ingest/     파싱·OCR → PII 마스킹 → 분류·메타데이터
  index/      청킹 → 어휘(Kiwi) + 임베딩 색인
  extract/    실적 카드 구조화 추출
  retrieve/   하이브리드 검색(RRF) + 리랭킹
  generate/   LLM 연결·공고 스키마 추출·섹션별 생성
  verify/     검증기 3종 — 수치 대조 · 배점 커버리지 · 예산 계산
  eval/       평가 하네스
  app/        FastAPI 플랫폼(화면·DB·파이프라인·개발자 도구)
  graph/      지식 그래프
scripts/      번호가 순서 — 서빙(126)·학습본 이관(116)·측정(53·105·106)·반입(115)·점검(110·125)·배포(99)
configs/      학습 설정(configs/training) · 서빙 실측 기록(configs/serving.yaml)
tools/        한글 편집 에이전트(Windows COM) · installer/ 그 설치 프로그램
docs/         설계·측정·결정(decisions/)·논문(paper/)·주간 보고(weekly/)·모델 카드(model-cards/)
data/         산출물·문서 (저장소에 넣지 않음)
```

## 모델 4종 (축 B)

| 모델 | 베이스 | 역할 | 상태 |
|---|---|---|---|
| `ZZAIMY-Embed` | KURE-v1 | 조밀 검색 | v2 운영(ADR-0021) |
| `ZZAIMY-Rerank` | bge-reranker-v2-m3 | 후보 재정렬 | v1 운영(ADR-0020) |
| `ZZAIMY-Writer` | Qwen3.8-27B (dense, 멀티모달) | 섹션 생성 + 이미지 판독·검토·대화 | 베이스 서빙(NVFP4), 학습은 SFT → DPO |
| `ZZAIMY-Extract` | Qwen3-4B | 실적 카드 추출 | 예정 |

계획은 `docs/model-plan.md`. 학습 데이터는 `/dev/data` 데이터 공방에서 수치 검증 통과분만.
학습본 이관은 `scripts/116_ship_and_serve.sh`(DGX → 두 토르, 리랭커면 근거 하한 재측정까지).

## 표현 규칙

- 쓰는 말: "도메인 특화 모델 개발", "오픈웨이트 기반 도메인 적응", "검색 정확도 N%p 개선"
- 안 쓰는 말: "자체 개발 LLM", "독자 파운데이션 모델", "from scratch 학습"
- 표기는 `ZZAIMY` / `zzaimy` / 「짜이미」. 슬로건·이모지·볼드 남발·조문체는 쓰지 않는다
- 문서·화면은 담백한 서술형. 사용자 대상 설명은 쉬운 말로, 명령은 복붙 가능한 한 줄로

## 작업 태도

- 확신이 서지 않으면 추측으로 채우지 말고 질문한다. 판단이 갈리면 선택지와 트레이드오프를 제시한다
- 검증 안 된 것은 "미확인"으로 적는다
- 운영 작업은 백업 먼저. root가 필요하면 우회하지 말고 명령을 사용자에게 준다. 비밀번호는 다루지 않는다
- 문서화를 먼저 한다. 끝났다는 보고는 검증 결과와 함께
