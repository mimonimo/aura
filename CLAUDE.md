# ZZAIMY 캡스톤 — 작업 지침

영남이공대학교 국고사업 계획서 작성 지원 시스템. RAG + 도메인 특화 모델 4종.
**공고를 날실로, 우리의 실적을 씨실로.**

## 먼저 읽을 것

다른 세션·계정에서 이어받는다면 **`docs/HANDOFF.md`를 가장 먼저** 읽는다 —
현재 인프라 상태·남은 일·이어받는 절차. 그리고 `docs/context/working-notes.md`
— 축적된 사용자 선호·작업 규율·주의사항(세션 메모리를 저장소로 옮긴 것).

1. `PROJECT_BRIEF.md` — **모든 판단의 기준 문서.** 여기와 충돌하는 구현은 틀린 구현이다
2. `docs/capstone-plan.md` — 13주 압축 일정, 삭감 범위, 블로커
3. `docs/workflow.md` — 명세(교수님)와 구현(나)의 분업 규칙
4. `research/00-summary.md` — 기술 스택 추천
5. `research/02-system-overview.md` — 조감도·유즈케이스

판단이 필요할 때 `research/01-A`~`01-N`(영역별 리서치), `research/50-model-track.md`(모델 트랙),
`research/99-open-questions.md`(미해결 질문)를 참조한다.

## 상황 (2026-09-08 현행화)

- **기간**: 2026-09-01 ~ 11-30 구축·논문 완료, 12월 초 교내 캡스톤 발표회
- **장비**: 운영 = ESXi VM(CPU, 웹·검색·OCR 담당, 상시 가동) / 학습·서빙 = DGX
  확보 대기. **기존 Spark(211.xx)는 사용 금지** — 상세는 `docs/HANDOFF.md`
- **데이터**: 실물 표본은 `data/samples/` (git 제외). 교내 문서·규정집 반입 예정
- **이후**: 실제 학교 행정에 활용 예정. 데모가 아니라 실사용 시스템
- **현재 단계**: 플랫폼 가동 중 (접수·검토·검색·OCR·그래프·데이터 공방).
  파인튜닝은 베이스라인 측정 + DGX 확보 후

원안 10개월 7단계를 13주로 압축했다. 삭감했던 것 중 **그래프(ADR-0009로 부활)와
DPO(2주차 미팅에서 부활 — SFT 성립 후)**는 되살아났고, 전체 문서 확장·④지출
계열·P7 운영 이관은 여전히 범위 밖이다. 모델 4종은 유지한다.

## 절대 규칙

브리프 11장 금지사항. 어기면 되돌리는 비용이 크거나 프로젝트가 실패한다.

1. **수치는 인출하고 생성하지 않는다.** 생성된 모든 수치는 인출된 근거에 존재해야 한다.
   근거 없는 수치가 계획서에 들어가면 허위 기재로 사업 취소·환수·제재 사유가 된다
2. **★P3 베이스라인 측정 전에 파인튜닝을 시작하지 않는다.** 순서를 바꾸면 축 B의 성과를
   입증할 수 없고, 그 순간 논문의 기여가 사라진다
3. **PII 스크리닝은 인덱싱 이전에.** 사후 조치는 벡터DB 전체 재구축을 의미한다
4. **열람 등급 필터링은 검색 단계에.** 나중에 붙이기 매우 어렵다
5. **예산 계산에 LLM을 쓰지 않는다.** 결정론적 로직으로
6. **문서 4계열을 단일 인덱스로 통합하지 않는다.** 처리 경로를 분리한다
7. **파일럿 우선.** 계획서 20~30건 + 대응 결과보고서. 수만 장 전체로 시작하지 않는다
8. **모델에 지식을 주입하지 않는다.** 능력만 학습시킨다. 지식은 검색으로 공급
9. **Spark를 분산 추론 클러스터로 묶지 않는다**
10. **④ 지출 문서 계열을 초기 범위에 넣지 않는다**

## 저장소 규칙

**업스트림 파일을 수정하지 않는다** — `PROJECT_BRIEF.md`, `research/**`,
`docs/business-plan.md`는 교수님 소유이며 읽기 전용이다. 명세에 문제를 발견하면
고치지 말고 `docs/decisions/`에 ADR로 기록한 뒤 업스트림 Issue로 올린다.

**데이터를 커밋하지 않는다** — 실제 문서·파싱 결과·실적 카드는 `data/` 아래에만 둔다.
`.gitignore`로 차단돼 있지만 `git add` 전에 `git status`를 확인한다.
한 번 커밋되면 삭제해도 git 이력에 남는다.

**결정은 ADR로 남긴다** — `docs/decisions/0000-template.md` 사용.
브리프 12장 요구사항이자 모델 카드·논문 방법론의 원재료다.
교수님 판단이 필요한 건은 `상태: 제안`으로 둔다.

## 구조

```
src/zzaimy/
  ingest/     파싱·OCR → PII 마스킹 → 계열 분류·메타데이터
  index/      청킹 → 이중 벡터화(dense + Kiwi sparse) → 벡터DB 적재
  extract/    실적 카드 구조화 추출 (xgrammar 형식 보장 + 원문 대조 내용 보장)
  retrieve/   하이브리드 검색(RRF) + 리랭킹
  generate/   공고 스키마 추출 + 섹션별 생성 루프 (통짜 생성 금지)
  verify/     검증기 3종 — 수치 대조 · 배점 커버리지 · 예산 계산
  eval/       평가 하네스 (베이스라인 대비 개선폭)
```

## 모델 4종 (축 B)

| 모델 | 베이스 | 역할 | 지표 |
|---|---|---|---|
| `ZZAIMY-Embed` | KURE-v1 | 도메인 특화 임베딩 | Recall@k, MRR, nDCG |
| `ZZAIMY-Rerank` | bge-reranker-v2-m3 | 리랭킹 | 동일 |
| `ZZAIMY-Writer` | **Qwen3.8-27B** (2주차 미팅 확정, 멀티모달) | 섹션 생성, QLoRA SFT → DPO | 배점 반영률, 수치 환각률 |
| `ZZAIMY-Extract` | Qwen3-4B | 실적 카드 추출 | 필드 F1, 처리량 |

학습 순서: 베이스라인 측정 → SFT → DPO(선호쌍 = 담당자 판정·재작성 이력).
학습 데이터는 `/dev/data` 데이터 공방에서 생성한다 — 수치 검증 통과분만.

학습 데이터 정제 규칙: **출력의 모든 수치가 입력 근거에 존재해야 한다.**
아니면 해당 학습 쌍을 폐기한다. 이걸 건너뛰면 모델이 수치를 외워서 지어낸다.

## 지금 할 일

현재 우선순위와 남은 작업은 **`docs/HANDOFF.md` §4**와 `docs/dev-now.md`가
기준이다 (W1~W2 지시서는 완료 — `docs/instructions/W1-W2.md`는 이력).

1. **개체 그래프 2단계** — 기준 문서에서 사업·부서·연도·실적 개체 추출
   (온톨로지 v2), 그래프·검색 확장에 연결
2. **문서·규정집 반입 대응** — 인제스트 → 데이터 공방 → 골드 세트.
   품질 문제는 `docs/quality-system.md` 5계층 체계로 처리
3. **DGX 확보 시** — Qwen3.8-27B 서빙 연결(VLLM_BASE_URL) → 베이스라인
   측정 → SFT
4. **한글(HWPX) 편집 에이전트** — `docs/notes/hwpx-agent-skills.md` 참조
5. UI/UX 개선 — 전수 조사 결과(`docs/notes/uiux-audit.md`) 반영

## 확정 필요 (막힌 것)

| 항목 | 상태 |
|---|---|
| 개인정보 부서 검토 (#11) | **대기 — 실물 반입·파일럿의 전제** |
| DGX 접속 | 제공자 확인 대기 |
| 문서 열람 등급 체계 (#7) | **본 인덱싱 전 확정 필수** |
| VM 아웃바운드 개방 | 이그레스 실전송·외부 참조에 필요 (관리자 요청) |

해소된 것: Writer 베이스(#5 → Qwen3.8-27B 확정), 파서 bake-off(ADR-0007),
P1 문서 5종(작성 완료), 단독 작업 체계(ADR-0005).

## 표현 규칙

- 사용 가능: "도메인 특화 모델 개발", "오픈웨이트 기반 도메인 적응", "검색 정확도 N%p 개선"
- **사용 금지**: "자체 개발 LLM", "독자 파운데이션 모델", "from scratch 학습"
- 표기는 `ZZAIMY` / `zzaimy` / 「짜이미」로 통일
- 시스템의 위치는 **70% 초안 + 사람 검수 전제**. 완성본 생성기가 아니다

## 작업 태도

브리프 0장·12장의 지시를 그대로 따른다.

- 확신이 서지 않으면 추측으로 채우지 말고 질문한다
- 판단이 갈리면 임의로 정하지 말고 선택지와 트레이드오프를 제시한다
- 검증 안 된 것은 "미확인"으로 표기한다. 그럴듯하게 지어내지 않는다
- 문서화를 먼저 한다
