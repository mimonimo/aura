# 실제 값은 .env에 두고 절대 커밋하지 않는다 (.gitignore로 차단됨)

# 데이터 루트 — 실제 문서·파싱 결과는 전부 이 아래에만 둔다
ZZAIMY_DATA_DIR=./data

# LLM 서빙 (TASK-02에서 확정)
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_API_KEY=dummy

# 벡터 DB (미해결 질문 #8 — Qdrant vs pgvector 확정 전)
VECTOR_DB_URL=http://localhost:6333
