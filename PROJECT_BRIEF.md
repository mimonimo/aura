# 프로젝트 브리프 v3 — 「짜이미」 교내 행정문서 처리 플랫폼

> 판단의 기준 문서다. 여기와 충돌하는 구현은 틀린 구현이다.
> 원안은 교수님이 2026-08에 준 브리프 v2(국고사업 계획서 작성 지원 시스템)였고, 그 문서로
> 리서치(`research/`)와 설계(`docs/`)를 마친 뒤 2026-09-22 우리 실정에 맞춰 이 판으로 고쳤다.
> 원안 전문은 git 이력(2026-09-01 커밋)에 있다. 지금 상태는 `docs/HANDOFF.md`·`docs/dev-now.md`,
> 결정과 근거는 `docs/decisions/`가 정본이며 이 문서는 그것을 되풀이하지 않는다.

---

## 이름과 표기

「짜이미」 / `ZZAIMY`. 「짜임」에 '-이'를 붙인 이름이고 Y는 영남이공대학교의 이니셜을 겸한다.
짜는 일은 없는 것을 지어내는 것이 아니라 있는 실을 엮는 것이다 — 이 시스템은 문장을 창조하지
않고 이미 존재하는 근거(공고의 요구와 기관의 실적)를 엮어 배치한다. 5.3항(수치는 생성하지 않고
인출한다)이 이름에 들어 있다.

| 용도 | 표기 |
|---|---|
| 공식 명칭 (보고서·발표) | 「짜이미」 / `ZZAIMY` |
| 저장소 | `aura` (GitHub mimonimo/aura), 패키지 `zzaimy` |
| 모델명 | `ZZAIMY-Embed` / `ZZAIMY-Rerank` / `ZZAIMY-Writer` / `ZZAIMY-Extract` |

`ZZAIMY`, `zzaimy` 외의 표기(`Y-ZZAIM`, `ZAIM`, `ZZAIMI` 등)는 쓰지 않는다.

---

## 1. 배경과 문제

영남이공대학교 행정 부서는 국고사업 공고 분석과 계획서 작성, 채용·입학 서류 검토, 규정 확인처럼
문서를 읽고 정리하는 반복 업무를 한다. 국고사업 문서는 수만 장 규모의 PDF·HWP로 파일 서버에
쌓여 있지만 활용 수단이 없고, 새 공고가 나올 때마다 담당자가 과거 문서를 손으로 뒤져 계획서를
처음부터 다시 쓴다. 이 문서들은 개인정보와 기관 내부 정보를 담고 있어 외부 클라우드 AI에 올릴
수 없다. 그래서 교내 장비 위의 로컬 모델로 같은 일을 하는 시스템을 만든다.

시작 도메인은 국고사업이고, 같은 파이프라인(반입·판독·마스킹·검색·검토·초안)으로 채용·입학
서류 검토와 규정 응답까지 넓혔다. 전결·결재 같은 절차는 기존 업무 포털이 처리하므로 재현하지
않는다.

### 보유 문서의 구성 (네 가지 계열)

| 계열 | 내용 | 시스템에서의 역할 |
|---|---|---|
| ① 공고·지침류 | 사업 공고문, 운영 지침, 평가 기준표, 교내 규정 | 요구사항 정의 — 계획서 뼈대와 배점 체계, 검토·응답의 근거 |
| ② 사업계획서 | 과거 제출한 사업계획서 (선정/미선정 포함) | 논리 구조·표현 자산 + 생성 모델 학습 데이터 |
| ③ 프로그램 계획서/결과보고서 | 개별 프로그램의 계획과 성과 | 실적 데이터 — 정량 성과 수치의 원천. 최우선 가치 |
| ④ 지출 문서 | 기자재 구매, 환경개선, 재료비 등 집행 문서 | 단가 레퍼런스. 이번 범위 밖 |

### 핵심 인식 — 보유 문서는 그 자체로 지도학습 데이터셋이다

② 사업계획서는 "요구사항 + 근거자료 → 완성된 섹션"의 정답 쌍, ③ 결과보고서는 "PDF → 정형
실적 데이터"의 정답 쌍, 전체 문서는 합성 질의를 통한 "질문 → 정답 조각" 검색 학습 쌍이다.
여기에 플랫폼에서 쌓이는 담당자 판정·재작성 이력이 선호 학습(DPO)의 재료로 더해진다.
사람의 수작업 라벨링 없이 학습 데이터를 확보한다는 전제를 설계 전반에서 우선한다.

---

## 2. 목표 — 두 개의 축

둘 다 필수다.

### 축 A — 실용 시스템

문서 반입 → 판독 → 개인정보 마스킹 → 검색 → 검토·초안 작성에 이르는 전 경로를 실제 학교 행정에
쓰는 수준으로 구현한다. 새 공고문을 넣고 방향 몇 가지를 지정하면 기관의 실제 실적에 근거한
계획서 초안이 나온다.

성공 기준: 초안이 공고의 평가지표·배점 항목을 빠짐없이 반영한다. 모든 정량 수치가 보유 문서에서
인출되었고 출처(문서·쪽)가 추적된다. 초안 작성 시간이 2주에서 2~3일로 준다.
목표가 아닌 것: 사람 검수 없는 제출 완성본. 목표는 "70% 초안 + 사람 검수"다.

### 축 B — 도메인 특화 모델 개발

오픈웨이트 모델을 베이스로 교내 문서 도메인에 특화된 모델을 학습하고 베이스 대비 개선폭을
정량 입증한다. 검색 인덱스만으로는 성과 요건을 채우지 못하므로 학습된 가중치를 산출물로
확보하되, 실제로 시스템 성능을 올리는 방식으로만 한다(성과 요건만을 위한 학습은 하지 않는다).

성공 기준: 베이스 대비 개선폭이 재현 가능하게 측정된다. 모델 카드·평가 보고서·버전 이력이 있다.

---

## 3. 최종 산출물과 상태 (2026-09-22)

| # | 산출물 | 상태 |
|---|---|---|
| 1 | 문서 파싱·정제 파이프라인 (④ 계열 제외) | 가동 — 디지털 PDF 글자층 직독, 스캔·사진은 Writer 이미지 판독, MinerU 폴백, HWP 리더(ADR-0007·0014·0016) |
| 2 | 실적 카드 DB (정형 테이블) | 예정 — 교내 결과보고서 수령 후 |
| 3 | 하이브리드 검색 인덱스 | 가동 — 어휘(Kiwi) + 임베딩 RRF, 후보 20 리랭킹. 문서 194건·조각 4,284개 |
| 4 | 섹션별 계획서 생성 파이프라인 + 검증기 3종 | 가동 — 실물 공고로 재보정 대기 |
| 5 | `ZZAIMY-Embed` | v2 운영 채택(ADR-0021) |
| 6 | `ZZAIMY-Rerank` | v1 운영 채택(ADR-0020) |
| 7 | `ZZAIMY-Writer` (LoRA) | 베이스 서빙 중, 학습은 실물 문서·베이스라인 뒤 |
| 8 | `ZZAIMY-Extract` | 예정 |
| 9 | 모델 카드 | Embed v2·Rerank v1 작성(`docs/model-cards/`) |
| 10 | 평가 보고서 | 검색은 `data/platform/eval/` 기계 산출물 + `docs/paper/실험-로그.md` |
| 11 | 운영 서버 요구사양 | 약식 — 실측 로그 기반 부록으로 대체(capstone-plan §2) |

---

## 4. 환경과 제약

### 장비 (ADR-0023)

| 장비 | 역할 |
|---|---|
| 운영 서버 — 학과 ESXi VM (CPU, GPU 없음, 상시 가동) | 웹·검색·OCR·문서관리. 담당자가 쓰는 화면 |
| 젯슨 AGX 토르 02 (통합 메모리 122GB) | Writer 27B 서빙 — 대화·초안(사람이 기다리는 일) |
| 젯슨 AGX 토르 03 (통합 메모리 122GB) | Writer 27B 서빙 — 반입 검토·이미지 판독(배치) + 임베딩·리랭커 서비스 |
| DGX | 학습 전용. 학습본은 bf16 병합 → NVFP4 양자화 → 두 토르로 이관 |
| Windows PC + 한글 | 담당자 실사용, 한글 편집 에이전트 |

초기 검증(9월 1주)에 쓴 DGX Spark는 사용하지 않는다. 젯슨은 메모리 대역폭이 속도를 정하므로
무게를 줄이는 것이 곧 속도다 — 정밀도 우선순위는 NVFP4 > FP8 > bf16이고, 채택은 같은 입력에서
내용이 같음을 확인한 뒤에만 한다(실측 3~4배).

### 필수 제약

- 온프레미스. 교내·사용자 문서는 어떤 경우에도 밖으로 나가지 않는다. 유일한 예외는 공개
  수집 문서의 판독에 한해 상용 API를 쓰는 것이며(ADR-0024, 제안), 그것도 화면에서 지정한
  용도에만 붙는다.
- 한국어 문서 100%. 모든 구성요소는 한국어 성능 기준으로 고른다.
- 교내 전용. 대외 서비스·상용화 없음. 실용성과 운영 단순성을 우선한다.
- 라이선스 검토. AGPL·상용 제한·비상업 조항이 있는 것은 채택 전 위험으로 보고한다.
  베이스 모델은 Apache 2.0 계열(Qwen·KURE·bge)이라 `ZZAIMY-*` 독자 명명이 가능하다.
- 모델은 계획한 4종만 쓴다. 문서에 없는 모델을 편의로 끼워 넣지 않는다(5.7).

---

## 5. 설계 원칙

### 5.1 바닥부터 만들지 않는다
파싱, 청킹, 벡터 검색, 리랭킹, 학습 프레임워크, 평가 하네스는 성숙한 오픈소스를 조립한다.
직접 만드는 것은 도메인 고유 로직 — 공고→요구사항 스키마, 실적 카드 추출, 섹션별 생성,
배점 대조 검증, 학습 데이터 생성, 반입 규칙 — 뿐이다.

### 5.2 파인튜닝의 역할을 정확히 한정한다
문서를 학습시켜 모델에 지식을 주입하지 않는다. 학습하는 것은 검색 성능(임베딩·리랭커), 문체·
구조(섹션 생성), 추출 정확도(실적 카드)다. "무엇을 쓸 것인가"는 검색이, "어떻게 쓸 것인가"는
파인튜닝이 맡는다. 규정처럼 바뀌는 지식은 검색으로 공급한다(2주차 미팅).

### 5.3 숫자는 생성하지 않고 인출한다
없던 실적을 지어내면 허위 기재로 사업 취소·환수·제재 사유가 된다. 수치 자리는 출처와 함께
채우고, 근거가 없으면 빈칸으로 남겨 표시한다. 프롬프트가 아니라 파이프라인 구조에서 강제한다.
학습 데이터도 같은 기준이다 — 출력의 모든 수치가 입력 근거에 있어야 하고 아니면 그 쌍을 버린다.

### 5.4 통짜 생성을 하지 않는다
계획서는 섹션 단위 루프로 만든다.

### 5.5 평가셋과 베이스라인이 먼저다
베이스라인 없이 파인튜닝하면 개선을 주장할 수 없다. 측정은 운영과 같은 설정(서빙 장비·후보 수)
으로만 하고, 설정 없이 띄운 측정은 무효다.

### 5.6 문서 계열의 처리 경로를 분리한다
같은 PDF라도 역할이 다르다. 기준 문서(규정·공고·지침)와 접수 문서(개인정보 포함)는 마스킹·
저장·검색 경로가 다르며 단일 인덱스로 합치지 않는다.

### 5.7 계획한 모델만, 채택은 측정으로
서빙·판독에 쓰는 모델은 7장의 4종이다. 새 후보(양자화 판, 다른 베이스)는 먼저 ADR에 올리고
같은 입력의 대결 수치로만 채택한다.

### 5.8 사례에 맞춘 규칙을 넣지 않는다
특정 문서·오타에 맞춘 예외 규칙은 금지다. 같은 부류의 모든 입력에 적용되는 일반 메커니즘(모델·
학습·전처리·일반 교정)으로만 성능을 올린다. 품질 문제는 발생 계층에서 막는다(`docs/quality-system.md`).

### 5.9 검증되는 자동화
화면과 보고서의 수치는 기계 산출물에서만 온다. 새 규칙은 실제 반입 경로의 자가 점검으로 확인하고,
수동 단계는 스크립트·설정으로 없앤다. 새 것을 넣으면 기존 검증(테스트·스모크·PII·측정)을 다시 돌린다.

---

## 6. 시스템 구성

### 플랫폼
FastAPI + SQLite + 서버사이드 템플릿. 업무 영역(국고사업·채용·입학·일반) → 프로젝트(사업·공고·
학년도 단위) → 문서 접수·검토·판정·초안의 계층. 담당자·개발자 계정 분리. 운영 서버에서 systemd
자동시작, 오프라인 모델 캐시로 자립 동작. 개발자 화면(/dev)에 데이터 열람·데이터 공방·모델 학습·
품질·PII 점검·논문 자료·변경 이력이 있다.

### 반입
파일 이름이 아니라 본문 제목으로 이름을 정하고(붙임 표시·기호·표 줄·발신처 줄은 제목이 아니다),
같은 내용은 한 번만 들이며, 접수 문서는 파싱 직후 6종(주민·전화·이메일·계좌·사업자·성명) 마스킹
뒤에만 저장·전달한다. 기준 문서는 판단 근거이므로 원문을 유지한다(ADR-0006).

### 생성 루프

```
1. 공고 파싱      → 목차 골격 + 평가지표 + 배점 + 분량 제약 + 예산 기준을 정형 스키마로 추출
2. 사용자 입력    → 핵심 컨셉, 차별화 포인트, 예산 규모
3. 섹션별 루프    → 근거 인출(실적 카드 + 접수 자료 조각의 의미 유사 상위) → 생성 → 인출된 수치만 썼는지 검증
4. 배점 대조 검증  → 배점 항목별 반영 여부 — 키워드 판정 + 임베딩 의미 판정(ADR-0015), 누락 보고
5. 예산표 생성    → LLM 금지. 단가 DB + 계산 로직
6. 출력           → 섹션별 출처 주석이 붙은 초안. docx·pdf·md·hwpx, 한글은 COM 에이전트(ADR-0014)
```

### 실적 카드 DB
결과보고서에서 추출하는 정형 테이블(사업명·연도·예산·참여인원·정량성과·소관부서·장비·출처).
구조화 추출 문제이며 `ZZAIMY-Extract`의 대상이다. 교내 결과보고서 수령 후 구축한다.

### 지식 그래프
기준 문서·접수 문서·프로젝트를 노드로, 근거·소속·조문 참조·의미 유사를 간선으로 잇는 경량
온톨로지(ADR-0009). 문서 사이 연관은 규칙이 아니라 학습된 임베딩의 의미 유사로 판정한다
(ADR-0015). 개체(사업·부서·연도·실적) 계층은 2단계 진행 중(ADR-0010).

### 외부 참조 관문
외부 모델을 부를 때는 PII·기관 식별자 세척 → 위험 판정 → 승인 큐 → 감사 기록을 거친다
(ADR-0008·0019). 실전송은 용도 지정과 키 등록 뒤에만 가능하다.

---

## 7. 모델 개발 트랙 (축 B)

네 모델 모두 실제 시스템 성능 개선에 기여해야 한다.

| 모델 | 베이스 | 학습 | 지표 | 상태 |
|---|---|---|---|---|
| `ZZAIMY-Embed` | KURE-v1 | 합성 질의 대조학습, 문서 단위 홀드아웃 | Recall@k, MRR | v2 채택 — 색인과 질의 임베딩을 같은 모델로(ADR-0021) |
| `ZZAIMY-Rerank` | bge-reranker-v2-m3 | 운영 하이브리드가 올린 후보 20개로 목록 학습 | 리랭킹 후 Recall@k | v1 채택 — 홀드아웃 R@1 0.649→0.711(ADR-0020) |
| `ZZAIMY-Writer` | Qwen3.8-27B (dense, 멀티모달 — 2주차 미팅 확정) | QLoRA SFT → DPO(선호쌍 = 담당자 판정·재작성 이력) | 배점 반영률, 수치 환각률(0 수렴), 사람 평가 | 베이스 서빙 중(NVFP4). 이미지 판독·검토·대화·초안이 모두 이 모델 |
| `ZZAIMY-Extract` | Qwen3-4B | 교사 추출 → 사람 검수 → SFT, xgrammar 병행 | 필드 F1, 처리량 | 예정 |

- 학습은 DGX에서, 서빙은 토르에서. 학습본 이관은 `scripts/116_ship_and_serve.sh`로 자동이다.
- 학습 데이터는 `/dev/data` 데이터 공방(검토 의견·초안·대화 → JSONL)에서 만들고 수치 검증 통과분만 쓴다.
- 채택 기준은 "운영 경로 위의 개선"이다. 단독 지표만 오른 판(Embed v1)은 채택하지 않았다.
- 산출물: 모델 카드(베이스·라이선스·데이터·하이퍼파라미터·GPU-시간), 평가 보고서(개선폭·재현 절차), 버전 이력.
- 대외 표현: "도메인 특화 모델 개발", "오픈웨이트 기반 도메인 적응", "검색 정확도 N%p 개선"은 쓰고,
  "자체 개발 LLM", "독자 파운데이션 모델", "from scratch 학습"은 쓰지 않는다.

---

## 8. 일정 (13주, 2026-09-01 ~ 11-30, 12월 초 교내 발표회)

| 주차 | 내용 | 상태 |
|---|---|---|
| W1~2 (9/1~9/14) | 설계 문서 5종, 장비 검증, 파서 확정, 운영 서버 이전, 플랫폼 가동 | 완료 |
| W3~5 (9/15~10/5) | 문서 반입·파싱, 검색 학습본, 서빙 장비 구성, 평가셋 | 진행 — 공개 문서 194건 반입, Embed v2·Rerank v1 채택, 두 토르 NVFP4. 교내 실물 문서·골드셋 대기 |
| W6~8 (10/6~10/26) | 실적 카드 DB, 생성 모델 베이스라인 측정 | 게이트 — 베이스라인 전 Writer 학습 금지 |
| W9~10 (10/27~11/9) | 생성 파이프라인·검증기 실물 보정, 실공고 end-to-end | |
| W11~12 (11/10~11/23) | Writer SFT → DPO, Extract 학습, 개선폭 측정, 모델 카드 | |
| W13 (11/24~11/30) | 평가 정리, 논문, 발표 | |

원안의 10개월 7단계를 13주로 압축했다. 삭감했다가 되살린 것은 지식 그래프(ADR-0009)와
DPO(2주차 미팅)이고, 전체 문서 확장·④ 지출 계열·운영 이관은 여전히 범위 밖이다.

---

## 9. 금지사항

1. 근거 없는 수치를 생성하거나 학습 데이터에 남기는 것
2. 베이스라인 측정 없이 파인튜닝을 먼저 하는 것
3. PII 스크리닝을 인덱싱 뒤로 미루는 것 (사후 조치 = 색인 전체 재구축)
4. 열람 등급 필터를 검색 단계 밖에 두는 것
5. 예산 계산에 LLM을 쓰는 것
6. 문서 계열을 단일 인덱스로 합치는 것
7. 파일럿 범위(계획서 20~30건 + 결과보고서)를 건너뛰고 수만 장으로 시작하는 것
8. 모델에 지식을 주입하는 것 — 능력만 학습, 지식은 검색으로
9. ④ 지출 문서 계열을 이번 범위에 넣는 것
10. 계획 문서에 없는 모델을 쓰거나, 특정 사례에 맞춘 규칙으로 성능을 올리는 것
11. 검증하지 않은 수치를 사실로 적는 것 — 출처가 없으면 "미확인"
12. 실제 문서·파싱 결과·비밀번호·키를 저장소에 넣는 것

---

## 10. 작업 방식

- 결정은 ADR(`docs/decisions/`)로 남긴다. 결론만 바꾸고 근거를 안 남기는 방식은 쓰지 않는다.
- 문서화를 먼저 한다. 새 것을 넣으면 문서를 갱신하고 기존 검증을 다시 돌린다.
- 판단이 갈리면 임의로 정하지 말고 선택지와 트레이드오프를 제시한다. 확신이 없으면 묻는다.
- 운영 작업은 백업 먼저. root가 필요한 일은 우회하지 않고 명령을 사용자에게 준다.
- Codex·Claude 공동 작업 절차는 `docs/collaboration/README.md`. 주간 보고는 플랫폼의 논문 자료 화면에서 만든다.

---

## 11. 확정이 필요한 것 (학교)

| 항목 | 무엇이 막히나 |
|---|---|
| 교내 실물 문서 표본 | 파일럿, 골드셋, Writer·Extract 베이스라인과 학습 |
| 개인정보 부서 검토 | 실물 반입의 전제 |
| 문서 열람 등급 체계 | 본 색인 전 확정 필수 — 사후 부여는 전체 재색인 |
| DGX 셸 계정 | 학습 실행. 지금은 모델 호출만 된다 |
