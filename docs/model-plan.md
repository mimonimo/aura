# 모델 개발 계획 (축 B)

> 상태: **작성됨 — 승인 대기** (P1 산출물, 브리프 9장 2번)
> 근거: `research/50-model-track.md`를 이 프로젝트의 결정으로 확정 기록.
> ★게이트: **P3 베이스라인 측정(W8) 전에는 어떤 학습도 시작하지 않는다.**

## 0. 범위 확정

- 모델 4종 전부 수행한다 (13주 압축에서도 유지 — capstone-plan §2).
- **DPO 선호최적화는 제외** — SFT까지가 상한. 논문에 "향후 과제"로 기재.
- 베이스 모델은 전부 MIT/Apache 2.0 계열 → `ZZAIMY-*` 독자 명명 가능.
  단, 질문 #5(Kanana 성능 트레이드오프)·#6(Qwen 기관 수용성) 답변이 W2까지
  필요하다 — **상태: 미결**.

## 1. 학습 데이터 생성 파이프라인 (모델별)

### ZZAIMY-Embed (베이스: KURE-v1, 대안 BGE-M3 — 시작점 A/B 병행)

```
파일럿 코퍼스 청크 → 합성 질의 생성 (로컬 LLM, 질의 유형 3종: 실무형/공고 요구사항형/키워드형)
 → 라운드트립 품질 필터 (생성 질의로 원청크가 상위 검색되는지 자기 검증)
 → 하드 네거티브 채굴: hn_mine + 도메인 규칙("같은 사업 다른 연도" 함정을 명시 학습)
 → (질의, 정답 청크, 네거티브) 수만 쌍
```

프레임워크: FlagEmbedding 1안 (hn_mine 내장. P2 시점 스모크로 확정 —
sentence-transformers는 W1 스모크 통과 실측 있음, 폴백 확보됨).

### ZZAIMY-Rerank (베이스: bge-reranker-v2-m3)

Embed와 **동일 데이터 자산 재활용** (추가 생성 비용 0). cross-encoder 순위 학습.

### ZZAIMY-Writer (베이스: Qwen3.5-35B-A3B, MoE — 질문 #5/#6 조건부)

```
과거 계획서 20~30건 → 목차 기준 섹션 해체 (수백~수천 쌍)
 → retro-fill 역구성: 입력 = [공고 요구사항+평가지표] + [인용 실적의 역검색 근거 블록] + [방향성 요약]
 → 【수치 환각 방지 정제 — 절대 규칙, 아래 §2】
 → (입력, 출력=실제 제출 섹션 원문) 쌍 + 사업 단위 홀드아웃 분리
```

학습: QLoRA 4bit SFT, LLaMA-Factory (W1 스모크 실측 완료, ADR-0001).
128GB 통합메모리 내 QLoRA 가능성은 30~40GB로 **추정** (ADR-0001 — 실측은 P5 진입 시).

### ZZAIMY-Extract (베이스: Qwen3-4B, 3후보 소규모 비교 후 확정)

```
파일럿 결과보고서 → 교사 모델(Writer 베이스 + xgrammar) 추출
 → 사람 검수 수백 건 (실적 카드 검수 UC-5와 동일 작업 — 이중 활용)
 → 검수 완료본으로 SFT. 서빙 시에도 xgrammar 병행 (형식 이중 보증)
```

## 2. 수치 환각 방지 — 학습 데이터 정제 규칙 (절대 규칙)

1. 출력(섹션 원문)의 모든 수치·고유 실적명을 기계 추출한다.
2. 입력 근거 블록에 없는 수치가 출력에 있으면: 근거를 역검색해 입력에 추가하거나,
   찾지 못하면 **해당 학습 쌍을 폐기한다**. 수치 마스킹 치환은 문체 왜곡 위험이
   있어 쓰지 않는다 — 폐기가 원칙.
3. 정제 통과율·폐기 사유 분포를 데이터 카드에 기록한다 (모델 카드 재료).
4. 이 정제기는 검증기 1종(수치 대조)과 **같은 코드를 공유**한다 — 학습과 서빙이
   동일 기준으로 수치를 판정하게 한다.

## 3. 학습 일정 (13주 압축 매핑)

| 주차 | 작업 | 게이트 |
|---|---|---|
| W3~W5 | 합성 질의 생성·정제 (파싱 완료분부터 축적) | 학습 시작 아님 — 데이터만 |
| W6~W8 | ★베이스라인 측정 (베이스 모델 그대로) | **W8 베이스라인 리포트 확정** |
| W8~W10 | Embed v0.1 → v0.2, Rerank v0.1 | 게이트 통과 후 즉시 |
| W9~W10 | (병행) retro-fill 배치 + 정제, Extract 교사 추출·검수 | |
| W11~W12 | Writer QLoRA v0.1 → 사람 평가 → v0.2, Extract SFT·처리량 측정 | |
| W12 | 모델 카드 4종 + 평가 보고서 확정 | |

GPU-시간 추정(모두 **추정**, 문헌 기반 — `research/50` §비용): Embed/Rerank 각
8~24h/회, Writer 24~72h/회, Extract 4~12h/회. Spark 1대 기준. W1 스모크로
프레임워크 동작은 확인했고, 실효 처리량 보정은 P2 소형 잡(1시간)으로 한다.

## 4. 평가 프로토콜

상세는 `eval-plan.md`. 원칙만 고정한다:

- 모든 개선폭은 **베이스라인 리포트(W8) 대비**로만 주장한다.
- 평가셋 분리는 문서·사업 단위 (같은 문서/사업이 학습·평가 양쪽에 못 간다).
- Writer 수치 환각률은 결정론적 대조기로 측정하며 **0 수렴이 합격선**이다.
- 각 모델 v0.x마다 학습 YAML·데이터 스냅샷 해시·평가 결과를 모델 카드에 기록.

## 5. 산출물

모델 카드 4종(베이스·라이선스 고지·데이터 규모·하이퍼파라미터·GPU-시간),
평가 보고서(개선폭·재현 절차), 버전 이력. 표현 규칙 준수: "도메인 특화 모델
개발" — "자체 개발 LLM" 표현 금지 (브리프 7.6).
