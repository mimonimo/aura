# 아키텍처 설계

> 상태: **부분 작성** — P1 산출물 (브리프 9장 1번). 열람 등급 필터링 절만 작성됨 (TASK-06)
> 근거 문서: `research/00-summary.md`, `research/02-system-overview.md`

## 열람 등급 필터링 (TASK-06에서 확정한 구조)

절대 규칙 4: **열람 등급 필터링은 검색 단계에 있다.** 구현 구조는 다음과 같다.

- 모든 문서 메타데이터(`src/zzaimy/ingest/schema.py`의 `BaseDocMeta`)는
  `access_level` 필드를 갖는다. 값은 문자열 enum(`AccessLevel`)이라
  벡터DB의 메타데이터 필터(Qdrant payload filter / pgvector WHERE 절)에
  **변환 없이 그대로** 쓸 수 있다.
- 인덱싱 시 청크마다 이 값이 페이로드로 함께 적재된다. 따라서 검색 쿼리는
  `access_level ∈ {사용자 허용 등급}` 필터를 항상 동반하며, 필터를 빼먹은
  검색이 없도록 retrieve 계층의 공개 API가 사용자 등급을 필수 인자로 받는다.
- **미분류 문서의 기본값은 가장 보수적인 `restricted`다.** 등급이 확정되지
  않은 문서가 검색 결과로 새는 사고를 구조적으로 막는다.
- 값 체계 자체(등급 명칭·단계 수)는 **미해결 질문 #7** 확정 후 교체한다.
  필드와 필터 경로를 지금 만들어 두므로 값 교체는 재인덱싱 없이 가능하지만,
  필드 자체를 나중에 추가하는 것은 전체 재인덱싱이다 — 그래서 지금 넣었다.

## 작성해야 할 것

- [ ] 전체 데이터 흐름
- [ ] 문서 4계열별 처리 경로 분리 (5.6 — 단일 인덱스 통합 금지)
- [ ] 실적 카드 DB 스키마
- [ ] 섹션별 생성 파이프라인
- [ ] 배점 대조 검증기
- [ ] 예산표 계산 로직 (LLM 생성 금지)

## P1에서 확정해야 할 미결 사항

| 항목 | 선택지 | 결정 근거 |
|---|---|---|
| 벡터 스토어 | Qdrant(2-스토어) vs pgvector(1-스토어) | 미해결 질문 #8 운영 인력 경험 |
| 파서 | MinerU 3.x vs Docling | 실물 bake-off (W1~W2) |
| 열람 등급 필터 | 검색 단계 필터링 구조 | 미해결 질문 #7 — **인덱싱 전 확정 필수** |
