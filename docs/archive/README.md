# 지난 측정 기록

기계가 한 번씩 뽑아 놓은 중간 측정 보고서를 모아 둔다. 현재 수치는 저장소 본문 쪽을 본다
(`docs/llm-rerank-eval.md`, `docs/retrieval-baseline-mini.md`, ADR).

지금은 평가 스크립트가 보고서를 `data/platform/eval/` 에 쓰고, 저장소에는 `--report` 를 붙일 때만
남긴다. 여기 있는 파일은 그 규칙 이전에 쌓인 것이다.
