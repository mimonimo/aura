# 실험 로그 (논문 원재료)

정리 2026-09-22: 절에 번호를 붙이고(1 시계열 표 → 6 서빙 정밀도), 9/15 시점의 "DGX 확보 후·LLM 연결 후" 판정 문구를 그 뒤 실제 결과로 바꿨다. 이전 정리 이력은 git 기록에 있다.

축 B(도메인 특화)와 파이프라인 판정 실험의 시계열 기록. 논문 표·방법론의
1차 색인이다. 각 행의 수치 정본은 판정 칸에 적은 리포트(docs/ 아래)이며,
리포트가 없는 값은 미확인으로 남긴다. 부정적 결과도 그대로 남긴다.
`논문-원재료.md`는 이 로그를 인용하며 수치를 따로 유지하지 않는다.

## 1. 실험 시계열 (09-01 ~ 09-14)

| 일자 | 실험 | 설정 | 결과 | 판정 · 정본 |
|---|---|---|---|---|
| 09-01 | 서빙 스택 확정 (DGX Spark) | vLLM nightly(다이제스트 고정), Qwen3.5-35B-A3B bf16 | 첫 토큰 87ms, 30.8 tok/s (dense Qwen3-4B 47ms·23.0 tok/s 대비 우위) | 채택 (ADR-0004). Writer 베이스는 09-08 미팅에서 Qwen3.8-27B로 변경 — 서빙 실측은 6절(09-21) |
| 09-02 | ZZAIMY-Embed v0 1차 리허설 (구 코퍼스) | KURE-v1, MNR 1ep, batch 8, CPU | 홀드아웃 수치 미확인 — 리포트가 09-03 재학습 결과로 덮어써짐(dev-changelog 09-02 09:34) | 리허설 — 사이클 완주 확인, 홀드아웃 편중 한계 명시 |
| 09-02 | MinerU hybrid-engine 대결 | 문서 173 실물(스캔형 매뉴얼), pipeline과 동일 입력 | 표 구조 동일(표 6·행 21), "Ⅱ유형"→"표유형" 오독, 본문 추출량 절반(2,423자 vs 4,804자) | 보류 — pipeline 유지 (notes/2026-09-02-ocr-engine-refs) |
| 09-03 | dots.ocr 대결 | 문서 173 p3/5/7, vLLM 도커 서빙 | 고유명사 오독 2회("예만사다리" 등), 영어 환각, 영역 처리 비일관 | 보류 — 환각 위험, MinerU 유지 (동상) |
| 09-03 | 검색 기준선 재측정 (재구성 코퍼스 1,236조각) | 합성 질의 826건(dev-changelog 09-03 08:22), 지표 정본(eval/retrieval) | 수치 미확인 — 09-04 재색인 후 정본 재측정으로 대체 | 아래 09-04 행이 기준선 |
| 09-03 | ZZAIMY-Embed 재학습 (신 코퍼스) | 학습 쌍 2,253, MNR 1ep, batch 8, CPU, 3차 시도 완주, 홀드아웃 96질의(편성표 문서 7건, 문서 단위 분리) | 홀드아웃 R@1 .219 vs 베이스 .271, R@5 .615 vs .667, R@10 .833 vs .802, MRR .387 vs .429 | 미배포 — 합성 질의 SFT는 현 규모에서 이득 없음. 실데이터·층화 홀드아웃으로 재도전 (embed-v0-report) |
| 09-04 | 검색 미니 베이스라인 (정본) | 조각 1,236, 합성 질의 2,595건, KURE-v1 + Kiwi 명사·IDF | R@1 어휘 .338 / 임베딩 .611 / 하이브리드 동가중 .520 / 임베딩 우세 .595. MRR@10 .413 / .700 / .647 / .692. R@10 .567 / .877 / .879 / .877 | 기준선 확정 — 합성 자가 검색이라 절대치 낙관적, 비교·회귀용 (retrieval-baseline-mini) |
| 09-04 | 하이브리드 가중 스윕 | 재색인 후, w_a 0.2~1.2 격자, 질의 2,595건 | w_a 0.2~0.5 고원 MRR .692~.693(최적 0.3), 동가중(1.0) .647 | 서비스 기본값 0.4 유지 — 고원 안, 동가중 대비 +.045(최적 0.3은 +.046) (retrieval-weight-sweep, regulations.py HYBRID_W_A) |
| 09-04 | 리랭커 베이스라인 (bge-reranker-v2-m3) | 표본 300건, 하이브리드(동가중) 상위 10 재정렬 | R@1 .490→.623 (+.133), R@5 .813→.850, MRR .623→.716 | 도입 — 서비스 통합, ZZAIMY-Rerank 학습 전 기준선 확보. 속도는 09-14 행 (rerank-baseline) |
| 09-07~08 | OCR 어절 F1 벤치 (MinerU, 운영 VM CPU) | 디지털 PDF 렌더를 스캔처럼 OCR 후 원문 대조, 문서 5건 2~3쪽 | IACF 규정 4건 .7427~.8448(평균 ≈.81), #172 3쪽 .8134(교정 후 .7994) | 현행 기준선 — 도전자 비교 기준 (ocr-cer-bench · ocr-duel) |
| 09-08 | PaddleOCR-VL 0.9B 대결 (CPU) | VM 8 vCPU, transformers float32 | 1쪽 60분 내 미완(타임아웃), 4건×90분 미산출 | 측정 불가 — 09-21 토르 서빙 시도는 대화 템플릿 부재로 막힘, 판독은 Writer(27B)로 통일 (ocr-duel) |
| 09-08 | 한글 COM 에이전트 실장비 검증 | 한컴 automation 12.0, /dev/hwp 앱 | 서버 지시 → 한글 화면 반영 왕복 확인 | 채택 (ADR-0014, HANDOFF §4) |
| 09-08 | 파일럿 코퍼스 인제스트 | 공개 국고 문서 108건 수집(scripts/72·73) → scripts/74 | 87문서 · 1,693조각 · PII 68건, 0오류 | 완료 — 학습용, 기준 문서 인덱스와 분리 (dev-now 09-08) |
| 09-14 | 연관·반영 판정을 학습된 표현으로 | KURE-v1 CPU. 그래프 간선·초안 재료·배점 커버리지 | 그래프: 동의 절차끼리 .862, 장학 사업끼리 .842/.865, IACF 규정끼리 .905, 절차↔사업 미결합(개체 코사인은 .703 오결합). 커버리지: 절대 임계 0.62는 전부 누락, 돋보임 규칙으로 0/55→45/55점. 크로스인코더 시그모이드 .500~.534로 판별 불가 | 채택 (ADR-0015). 돋보임 폭 0.10은 합성 표본 — 실제 초안으로 재보정 대상 |
| 09-14 | 규정 조각 재분할 | 조 참조 미절단·빈 조각 병합·중복 제거·1,400자 상한 | 1,236→1,211조각(빈 0·중복 0), 재색인·개체 재추출 완료 | 적용 — 검색 재측정은 09-20 합성 질의 재생성 뒤 4절·5절 (HANDOFF §4) |
| 09-14 | 리랭커 쌍 길이 256 | 운영 VM CPU | 9.2s→4.6s/질의, top-1 일치 1.00 | 적용 (HANDOFF §4) |

## 2. 실패·사고 기록 (방법론의 재료)

- 학습 SIGKILL 원인 규명 2건: (1) 통합메모리에서 트레이너 CUDA 초기화 OOM
  → CUDA_VISIBLE_DEVICES= 로 CPU 강제, (2) 학습 중 동시 GPU 작업(MinerU·별도
  서빙)이 커널 OOM 킬 유발 → 학습 단독 점유 규칙 확립. 메모리 스파이크는
  MALLOC_ARENA_MAX=2·max_seq 512·batch 8로 완화(dev-changelog 09-02). 스텝
  시간 개선폭은 미확인
- 코퍼스 재구성 시 파생물 연쇄 무효화: 합성 질의 세트(chunk_id 기준)와
  임베딩 npz가 함께 무효 — "재구성 → 51 → 52 → 53" 체인 순서를 규칙화
- 디지털 PDF OCR 오판 실사고: MinerU auto가 디지털 문서를 OCR로 읽어
  "기초과학→기추과하" — 텍스트 레이어 감지 라우팅으로 원천 차단(ADR-0007)
- 운영 VM CPU 포화 실사고(09-08): PaddleOCR-VL 배치에 전체 코어 할당 → 약
  6시간 SSH·웹 응답 지연 — 무거운 작업은 코어 절반·낮은 우선순위·순차 처리
  규칙(HANDOFF §5)
- 오프라인 서버의 모델 로더 함정: 아웃바운드가 막힌 VM에서 허브 갱신 확인에
  멈춤 → 로더에 HF 오프라인 기본값(ADR-0015)
- 합성 질의 세트 유실: Spark → VM 이관 시 세트가 옮겨지지 않아 09-14 재분할
  후 검색 재측정이 막힘 → 09-20 LLM 연결 뒤 재생성(4절)
- 측정 무효 사고(09-21): 서빙 장비 설정 없이 측정을 띄워 리랭커·질의 임베딩이
  CPU 베이스로 물러남 → 보고서에 리랭커 출처·응답·폴백 수 기록, 서빙 장비가
  아니면 '운영 구성'이라 적지 않음(5절)
- 젯슨 GPU 메모리 함정 둘(09-21): 큰 파일을 받은 뒤 페이지 캐시가 CUDA 메모리를
  막아 vLLM 이 cuBLAS 오류로 죽음(root 로 캐시 비우기), 컨테이너 강제 종료가
  GPU 메모리 88GB 를 붙잡아 재부팅 필요 → 서빙 교체 스크립트는 정상 종료를
  기다린다(126)

## 3. OCR·복원 실험 (축 A 방법론)

- 복원 뷰 3안 비교(실물 렌더 검증): 블록 bbox 재구성(잘림·중복·왜곡 발생)
  → 글자 좌표 직독(pdfium charbox — 원리적 무오차, 단 표 괘선·서식 미표현)
  → 원본 렌더 배경 + 투명 텍스트 레이어(구글 OCR PDF 방식)로 확정.
  디지털=좌표 직독, 스캔=MinerU 줄 좌표(신뢰도≥0.5), 사진=보정 스캔+OCR 줄
  (ADR-0007)
- 표 복원: 디지털 PDF는 괘선 좌표와 글자층을 직접 읽어 병합·열 폭·텍스트를
  원본대로 복원(괘선 직독). 실물 공고문에서 OCR 오인식("직계존속정"보")이
  사라지고 원본과 같은 칸 비율로 복원됨을 확인(weekly/2026-09-08-w2)
- LLM 오타 교정의 게이트·범위 확장: 본문만 → 표 셀 포함, 라벨 기준 →
  실제 OCR 발생 기준. 행정 용어 사전 프롬프트로 맥락 없는 셀 교정률 개선
  ("주민등특번호→주민등록번호", "등의여부→동의여부" 실측 교정)

## 4. 검색 모델 학습·채택 (축 B, 2026-09-20)

- 리랭커 ZZAIMY-Rerank v1(bge-reranker-v2-m3 학습본): 운영 하이브리드가 실제로 올린 후보 20개에서 목록 단위
  학습, 문서 단위 홀드아웃 12개. 홀드아웃 R@1 0.649 → 0.711. 서빙 장비(토르 03 GPU)로 옮겨 질의당
  3.75초 → 0.07초. 근거 하한(RERANK_MIN)은 모델마다 다시 잼(ADR-0020)
- 임베딩 ZZAIMY-Embed v2(KURE-v1 학습본): 후보 진입률로 판정해 채택, 색인과 질의 임베딩을 같은 모델로
  묶어 서빙(ADR-0021). 후보 수 10 → 20(30은 이득 없음)
- 상황 질의(용어를 모르고 묻는 질문)의 22.7%는 두 검색 축 모두 상위 20 밖 — 리랭커·후보 수로는 못 넘는 몫

## 5. 문서함 재반입과 측정의 유효성 (2026-09-21)

- 문서함을 비우고 원본에서 재반입: 문서 194건(규정 8·교내 113·국고 공개 68·외부 공고 5), 기준 조각 4,254개.
  재반입 목록의 갈래 오류(교내 문서를 '문서 추출'로)와 폴더 누락(국고 공개 68건)으로 처음엔 조각이 300개였다
- 이름 규칙을 반입 단계로 옮김: 파일 이름이 test.pdf 여도 본문 제목으로, 같은 제목은 부제·접수번호로 구분.
  표 줄·공고번호 줄·발신처 줄·마크다운 표시는 제목이 아니다. 실제 반입 경로 자가 점검(scripts/125)으로 확인
- 측정 무효 사고: 측정을 .env.local 없이 띄워 서빙 장비 주소가 비었고, 리랭커·질의 임베딩이 VM CPU 베이스로
  떨어졌다. 세 번의 측정(운영 구성 R@1 0.55)이 모두 무효. 보고서에 리랭커 출처·응답·폴백 수를 항상 적고
  서빙 장비가 아니면 '운영 구성'이라 적지 않게 고침(K-48)
- 유효 측정(env 포함, 질의 1,026·표본 300): 어휘 R@1 0.430 · 임베딩 0.691 · 하이브리드 0.671 ·
  운영(하이브리드+리랭커) 0.763, R@10 0.927. 같은 표본 하이브리드 0.680 → 리랭커 0.763(+0.083).
  리랭커 서빙 장비 응답 300·폴백 0. 9/20 값(0.773, 질의 1,614·옛 코퍼스)과 비교는 참고용(재결선 안 된 행 267)
- 공개 수집 문서 73건을 마스킹 대상으로 바꿔 재처리 → 잔여 PII 208건 → 0건(스모크 PASS). '박사 학위'의 '박사'를
  성명으로 잡던 오탐 제거

## 6. 이미지 판독 모델과 서빙 정밀도 (2026-09-21~22)

- 판독 모델 비교(문서 422, 36쪽 스캔 매뉴얼, 글자층 65.7% 깨짐): gemma4(계획 외) 5쪽 21초·없는 문장 생성·줄 반복,
  Qwen3.8-27B bf16 128초·원문 그대로. 계획대로 Writer(27B)로 통일, gemma4 제거
- 27B bf16 처리량(토르 02, 젯슨): 동시 3요청 생성 합계 14 토큰/초(요청당 ~5). 검토 1건 3분, 판독 1쪽 1~2분.
  두 토르 분담(02 대화·초안, 03 검토·판독·검색)과 정밀도 우선순위 FP4 > FP8 > bf16 로 결정(ADR-0023)
- NVFP4: 기본 젯슨 vLLM 0.19 빌드는 양자화된 lm_head 를 못 받아 실패, Qwen3.8 전용 빌드로 적재 성공(요청 1개
  10.4 토큰/초). FP8 은 0.19 빌드에서 cuBLAS 실패. 젯슨 페이지 캐시가 CUDA 메모리를 막는 함정(root 로 캐시 비우기)
- 토르 02 교체(09-22): 대화·초안 서빙도 NVFP4 로, 요청 1개 10.9 토큰/초. 컨테이너 강제 종료가 GPU 메모리 88GB 를 붙잡아
  재부팅이 필요했던 사고 → 서빙 교체는 정상 종료를 기다린다. 두 서빙 서버가 같은 판(2절 사고 기록)
- 호출 제한 시간 180초는 27B 판독에 짧다(한 쪽 2분 초과 → 판독 꺼짐) → 판독·검토 600초. 검토 의견 상한 1,024 →
  2,048 토큰 + 이어 쓰기(잘림 193건 중 84건 → 0)

## 7. KURE-v2 다중 벡터 경로와 조각 잡음 원천 차단 (2026-09-22)

- 공개 KURE-v2(PyLate ColBERT, 토큰별 128차원)를 pylate 없이 옮겨 토르 03 에 색인(조각 4,284 → 토큰 846K, fp16 780MB,
  33초)과 MaxSim 서비스(:8017, 질의당 50~70ms)를 만들고 운영과 같은 질의 1,023건으로 쟀다. 단독 R@1 0.682(운영
  Embed v2 0.692), 후보 진입률 R@20 0.919(운영 0.935), 어휘 융합 0.634(운영 0.673), 리랭커 뒤 0.743(운영 0.737,
  표본 300 — 오차 안). 학습 전 공개 모델이 학습본에 근접하지만 운영 경로에서 이득이 없어 채택하지 않음(ADR-0025)
- 조각 잡음의 원인 셋을 일반 규칙으로 막음: 크기 분할이 만든 번호 줄 조각("3-2.", "Ⅴ.")을 재병합, 표 블록은 행 단위로
  묶어 행 중간 절단 금지, 본문 없는 표제 조각은 관문에서 제외(반입·승격·재분할이 같은 관문 index_ready). 실측: 실질
  20자 미만 조각 12 → 0, 관문 제외 41건. 재분할 뒤 조각 4,284 → 4,409(표 행 단위 분할로 늘어남)

