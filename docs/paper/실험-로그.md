# 실험 로그

축 B(도메인 특화)와 파이프라인 판정 실험의 시계열 기록이다. 논문의 표와 방법론 절이 여기서
출발한다. 수치의 정본은 각 행의 판정 칸에 적은 측정 기록이며, 기록이 없는 값은 확인하지 않은
것으로 남긴다. 실패한 실험도 그대로 둔다. 논문 원재료는 이 로그를 인용하고 수치를 따로 두지
않는다.

## 1. 실험 시계열 (9월 1일 ~ 14일)

| 일자 | 실험 | 설정 | 결과 | 판정 · 정본 |
|---|---|---|---|---|
| 09-01 | 서빙 스택 확정 (DGX Spark) | vLLM nightly(다이제스트 고정), Qwen3.5-35B-A3B bf16 | 첫 토큰 87ms, 30.8 tok/s (dense Qwen3-4B 47ms·23.0 tok/s 대비 우위) | 채택 (ADR-0004). Writer 베이스는 09-08 미팅에서 Qwen3.8-27B로 변경 — 서빙 실측은 6절(09-21) |
| 09-02 | ZZAIMY-Embed v0 1차 리허설 (구 코퍼스) | KURE-v1, MNR 1ep, batch 8, CPU | 홀드아웃 수치 미확인 — 리포트가 09-03 재학습 결과로 덮어써짐(dev-changelog 09-02 09:34) | 리허설 — 사이클 완주 확인, 홀드아웃 편중 한계 명시 |
| 09-02 | MinerU hybrid-engine 대결 | 문서 173 실물(스캔형 매뉴얼), pipeline과 동일 입력 | 표 구조 동일(표 6·행 21), "Ⅱ유형"→"표유형" 오독, 본문 추출량 절반(2,423자 vs 4,804자) | 보류 — pipeline 유지 (notes/2026-09-02-ocr-engine-refs) |
| 09-03 | dots.ocr 대결 | 문서 173 p3/5/7, vLLM 도커 서빙 | 고유명사 오독 2회("예만사다리" 등), 영어 환각, 영역 처리 비일관 | 보류 — 환각 위험, MinerU 유지 (동상) |
| 09-03 | 검색 기준선 재측정 (재구성 코퍼스 1,236조각) | 합성 질의 826건(dev-changelog 09-03 08:22), 지표 정본(eval/retrieval) | 수치 미확인 — 09-04 재색인 후 정본 재측정으로 대체 | 아래 09-04 행이 기준선 |
| 09-03 | ZZAIMY-Embed 재학습 (신 코퍼스) | 학습 쌍 2,253, MNR 1ep, batch 8, CPU, 3차 시도 완주, 홀드아웃 96질의(편성표 문서 7건, 문서 단위 분리) | 홀드아웃 R@1 .219 vs 베이스 .271, R@5 .615 vs .667, R@10 .833 vs .802, MRR .387 vs .429 | 미배포 — 합성 질의 SFT는 현 규모에서 이득 없음. 실데이터·층화 홀드아웃으로 재도전 (embed-v0-report) |
| 09-04 | 검색 미니 베이스라인 (정본) | 조각 1,236, 합성 질의 2,595건, KURE-v1 + Kiwi 명사·IDF | R@1 어휘 .338 / 임베딩 .611 / 하이브리드 동가중 .520 / 임베딩 우세 .595. MRR@10 .413 / .700 / .647 / .692. R@10 .567 / .877 / .879 / .877 | 기준선 확정 — 합성 자가 검색이라 절대치 낙관적, 비교·회귀용 (retrieval-baseline-mini) |
| 09-04 | 하이브리드 가중 스윕 | 재색인 후, w_a 0.2~1.2 격자, 질의 2,595건 | w_a 0.2~0.5 고원 MRR .692~.693(최적 0.3), 동가중(1.0) .647 | 서비스 기본값 0.4 유지 — 고원 안, 동가중 대비 +.045(최적 0.3은 +.046) (retrieval-weight-sweep, regulations.py HYBRID_W_A) |
| 09-04 | 리랭커 베이스라인 (bge-reranker-v2-m3) | 표본 300건, 하이브리드(동가중) 상위 10 재정렬 | R@1 .490→.623 (+.133), R@5 .813→.850, MRR .623→.716 | 도입 — 서비스 통합, ZZAIMY-Rerank 학습 전 기준선 확보. 속도는 09-14 행 (rerank-baseline) |
| 09-07~08 | OCR 어절 F1 벤치 (MinerU, 운영 VM CPU) | 디지털 PDF 렌더를 스캔처럼 OCR 후 원문 대조, 문서 5건 2~3쪽 | IACF 규정 4건 .7427~.8448(평균 ≈.81), #172 3쪽 .8134(교정 후 .7994) | 현행 기준선 — 도전자 비교 기준 (ocr-cer-bench · ocr-duel) |
| 09-08 | PaddleOCR-VL 0.9B 대결 (CPU) | VM 8 vCPU, transformers float32 | 1쪽 60분 내 미완(타임아웃), 4건×90분 미산출 | 측정 불가 — 09-21 토르 서빙 시도는 대화 템플릿 부재로 막힘, 판독은 Writer(27B)로 통일 (ocr-duel) |
| 09-08 | 한글 COM 에이전트 실장비 검증 | 한컴 automation 12.0, /dev/hwp 앱 | 서버 지시 → 한글 화면 반영 왕복 확인 | 채택 (ADR-0014, HANDOFF §4) |
| 09-08 | 파일럿 코퍼스 인제스트 | 공개 국고 문서 108건 수집(scripts/72·73) → scripts/74 | 87문서 · 1,693조각 · PII 68건, 0오류 | 완료 — 학습용, 기준 문서 인덱스와 분리 (dev-now 09-08) |
| 09-14 | 연관·반영 판정을 학습된 표현으로 | KURE-v1 CPU. 그래프 간선·초안 재료·배점 커버리지 | 그래프: 동의 절차끼리 .862, 장학 사업끼리 .842/.865, IACF 규정끼리 .905, 절차↔사업 미결합(개체 코사인은 .703 오결합). 커버리지: 절대 임계 0.62는 전부 누락, 돋보임 규칙으로 0/55→45/55점. 크로스인코더 시그모이드 .500~.534로 판별 불가 | 채택 (ADR-0015). 돋보임 폭 0.10은 합성 표본 — 실제 초안으로 재보정 대상 |
| 09-14 | 규정 조각 재분할 | 조 참조 미절단·빈 조각 병합·중복 제거·1,400자 상한 | 1,236→1,211조각(빈 0·중복 0), 재색인·개체 재추출 완료 | 적용 — 검색 재측정은 09-20 합성 질의 재생성 뒤 4절·5절 (HANDOFF §4) |
| 09-14 | 리랭커 쌍 길이 256 | 운영 VM CPU | 9.2s→4.6s/질의, top-1 일치 1.00 | 적용 (HANDOFF §4) |

## 2. 실패와 사고의 기록

방법론의 재료가 되는 실패들이다.

학습이 두 번 강제 종료됐다. 한 번은 통합 메모리 장비에서 학습 프로그램이 GPU를 초기화하다 메모리가
모자랐고, 한 번은 학습 중에 돌던 다른 GPU 작업이 커널의 메모리 회수를 불렀다. 앞의 문제는 학습을
CPU로 강제해 피했고 뒤의 문제는 학습이 장비를 단독으로 쓰는 규칙으로 막았다. 메모리 스파이크는
할당 영역과 시퀀스 길이, 배치를 줄여 완화했다.

코퍼스를 다시 구성하면 조각 번호에 묶인 합성 질의 세트와 임베딩 파일이 함께 무효가 된다. 재구성
뒤에는 질의 생성, 임베딩, 측정을 이 순서로 다시 돌리는 규칙을 두었다.

디지털 PDF를 OCR로 읽는 오판이 있었다. MinerU가 글자층이 있는 문서를 스캔으로 다뤄 "기초과학"을
"기추과하"로 읽었다. 글자층을 먼저 감지해 경로를 나누는 것으로 원천에서 막았다.

9월 8일에는 운영 서버의 CPU가 포화됐다. OCR 배치에 코어를 전부 할당해 여섯 시간 동안 접속과
웹 응답이 늦어졌다. 무거운 작업은 코어의 절반만 쓰고 우선순위를 낮춰 차례로 돌리는 규칙을 두었다.

외부 인터넷이 막힌 서버에서 모델 적재기가 허브의 갱신을 확인하려다 멈췄다. 적재기의 기본값을
오프라인으로 바꿨다.

합성 질의 세트가 Spark에서 운영 서버로 옮겨지지 않아 9월 14일 재분할 뒤의 재측정이 막혔다. 언어
모델을 연결한 9월 20일에 다시 만들었다.

9월 21일에는 측정이 세 번 무효가 됐다. 서빙 장비 설정을 읽지 않고 띄운 측정이 리랭커와 질의
임베딩을 CPU 베이스 모델로 조용히 물러나게 했고, 운영 구성 행이 실제보다 0.1 낮게 나왔다. 이후
보고서는 어느 리랭커가 답했는지와 폴백 수를 항상 적고, 서빙 장비가 아니면 운영 구성이라 부르지
않는다.

같은 날 젯슨에서 GPU 메모리 함정을 둘 겪었다. 큰 파일을 받은 뒤 페이지 캐시가 GPU 메모리를 막아
vLLM이 죽었고, 컨테이너를 강제로 끝내자 GPU 메모리 88GB가 돌아오지 않아 재부팅이 필요했다. 서빙
교체는 정상 종료를 기다리도록 바꿨다.

## 3. OCR과 복원 실험

복원 뷰는 세 가지 안을 실물 렌더로 견줘 정했다. 블록 좌표를 다시 배치하는 방식은 잘림과 중복,
왜곡이 생겼다. 글자 좌표를 직접 읽는 방식은 원리상 오차가 없지만 표의 괘선과 서식을 표현하지
못했다. 결국 원본 렌더를 배경으로 두고 그 위에 투명한 글자층을 얹는 방식으로 확정했다. 디지털
PDF는 좌표를 직접 읽고, 스캔은 MinerU의 줄 좌표를, 사진은 보정한 스캔에 OCR 줄을 쓴다.

표는 디지털 PDF의 괘선 좌표와 글자층을 직접 읽어 병합, 열 폭, 글자를 원본대로 복원한다. 실물
공고문에서 OCR 오인식이 사라지고 원본과 같은 칸 비율로 복원되는 것을 확인했다.

언어 모델의 오타 교정은 본문에서 표 셀까지 넓혔고, 판정 기준을 라벨에서 실제 OCR 발생으로
바꿨다. 행정 용어 사전을 프롬프트에 넣자 맥락 없는 셀의 교정률이 올랐다. "주민등특번호"가
"주민등록번호"로, "등의여부"가 "동의여부"로 고쳐졌다.

근거: ADR-0007, docs/weekly/2026-09-08-w2.md.

## 4. 검색 모델 학습과 채택 (9월 20일)

리랭커 학습본 ZZAIMY-Rerank v1은 운영 하이브리드가 실제로 올린 후보 20개에서 목록 단위로 학습했다.
문서 단위 홀드아웃 12건에서 R@1이 0.649에서 0.711로 올랐다. 서빙 장비 GPU로 옮기자 질의당 시간이
3.75초에서 0.07초로 줄었다. 근거 하한은 모델마다 다시 잰다.

임베딩 학습본 ZZAIMY-Embed v2는 후보 진입률로 판정해 채택했고 색인과 질의 임베딩을 같은 모델로
묶어 서빙한다. 리랭커가 싸진 덕에 후보 수를 10개에서 20개로 늘렸다. 30개는 더 낫지 않았다.

용어를 모르고 상황으로 묻는 질의의 22.7%는 두 검색 축 모두 상위 20에 넣지 못한다. 이 몫은
리랭커나 후보 수로는 줄지 않는다.

근거: ADR-0020, ADR-0021, docs/model-cards.

## 5. 문서함 재반입과 측정의 유효성 (9월 21일)

문서함을 비우고 원본에서 다시 반입했다. 문서 194건(규정 8, 교내 113, 국고 공개 68, 외부 공고 5),
기준 조각 4,254개다. 처음에는 교내 문서를 잘못된 갈래로 넣고 국고 공개 문서 폴더가 목록에 없어
조각이 300개뿐이었다.

이름 규칙을 반입 단계로 옮겼다. 파일 이름이 무엇이든 본문 제목으로 이름을 정하고 같은 제목은
부제나 접수번호로 구분한다. 표 줄, 공고번호 줄, 발신처 줄, 마크다운 표시는 제목이 되지 않는다.
실제 반입 경로로 자가 점검했다.

유효한 측정은 질의 1,026건과 표본 300건으로 어휘 R@1 0.430, 임베딩 0.691, 하이브리드 0.671, 운영
구성 0.763, 운영 R@10 0.927이었다. 같은 표본에서 리랭커가 하이브리드를 0.680에서 0.763으로 올렸고
서빙 장비 응답 300건에 폴백은 없었다.

공개 수집 문서 73건을 마스킹 대상으로 바꿔 다시 처리하자 잔여 개인정보가 208건에서 0건이 됐다.
"박사 학위"의 "박사"를 성명으로 잡던 오탐도 없앴다.

근거: data/platform/eval/retrieval-latest.json, docs/HANDOFF.md 4절.

## 6. 이미지 판독 모델과 서빙 정밀도 (9월 21~22일)

글자층의 65.7%가 깨진 36쪽 스캔 매뉴얼로 판독 모델을 비교했다. 계획에 없던 소형 모델은 5쪽을
21초에 읽었지만 없는 문장을 지어내고 줄을 반복했다. Qwen3.8-27B 원본은 128초가 걸렸지만 원문을
그대로 읽었다. 계획대로 27B로 통일하고 소형 모델은 뺐다.

27B 원본은 젯슨에서 동시 세 요청의 생성 합이 초당 14토큰, 요청당 5토큰 남짓이었다. 검토 한 건에
3분, 판독 한 쪽에 1~2분이 걸렸다. 두 토르로 역할을 나누고 정밀도 우선순위를 FP4, FP8, bf16 순으로
정했다.

NVFP4 판은 젯슨 기본 빌드에서 양자화된 출력층을 읽지 못해 실패했고 Qwen3.8 전용 빌드로 올라갔다.
요청 하나에 초당 10.4토큰이다. FP8 판은 기본 빌드에서 실패했다. 9월 22일에는 토르 02도 NVFP4로
바꿔 두 서빙 서버가 같은 판을 쓴다(초당 10.9토큰).

호출 제한 시간 180초는 27B 판독에 짧아 한 쪽이 2분을 넘기면 판독이 꺼졌다. 판독과 검토의 제한을
600초로 늘렸다. 검토 의견의 생성 상한을 1,024에서 2,048토큰으로 올리고 상한에 걸리면 이어 쓰게
하자 잘린 검토가 193건 중 84건에서 0건이 됐다.

근거: ADR-0023.

## 7. KURE-v2 다중 벡터 경로와 조각 잡음 (9월 22일)

공개 KURE-v2는 조각당 벡터 하나가 아니라 토큰마다 128차원 벡터를 내는 모델이다. 인코딩을 옮겨
토르 03에 색인(조각 4,284개, 토큰 84만 6천 개, 780MB, 33초)과 MaxSim 서비스(질의당 50~70ms)를
만들고 운영과 같은 질의 1,023건으로 쟀다. 단독 R@1 0.682(운영 임베딩 0.692), 후보 진입률 0.919
(운영 0.935), 어휘 융합 0.634(운영 0.673), 리랭커 뒤 0.743(운영 0.737, 표본 300건)이었다. 학습 전
공개 모델이 학습본에 근접하지만 운영 경로에서 이득이 없어 채택하지 않았다.

조각 잡음의 원인 셋을 일반 규칙으로 막았다. 크기로 나눈 뒤 생긴 번호 줄 조각은 다시 병합하고,
표 블록은 행 단위로 묶고, 본문 없는 표제 조각은 적재 관문에서 뺀다. 반입, 승격, 재분할이 같은
관문을 쓴다. 실질 20자 미만 조각이 12개에서 0개로, 표제 없는 조각이 1,516개에서 1,090개로 줄었다.
이어 공고와 계획서의 하위 불릿을 절 안에 두고 병합 셀의 반복을 없애자 조각이 3,503개가 됐고, 한
공고의 조각이 12개에서 8개로 줄며 절마다 표제가 붙었다. 재측정은 운영 R@1 0.723, R@10 0.907,
MRR 0.782(질의 1,020건, 표본 300건)다. 조각이 커진 만큼 합성 질의 기준 값은 0.027 내려갔지만
조각 하나가 절 하나를 담아 인용과 화면 가독성이 낫다. 스모크 점검은 통과했다.

근거: ADR-0025, data/platform/eval/retrieval-latest.json.

## 8. 반입 충실도 — 표 평문, 쪽 번호, 판본, 서류 갈래, 깨진 판독 (9월 22일)

사용자가 데이터 열람에서 본 것은 넷이었다. 서식 문서의 본문 첫 줄이 '예비군대대 | 예비군대대 | 예비군대대 |
복학원 | 복학원 …' 처럼 같은 말로 채워졌고, 250쪽짜리 문서의 조각에 쪽 번호가 없었으며, 기준 문서 목록에 같은
제목이 세 번씩 서 있었고, 문서가 공고인지 양식인지 구분이 없었다. 원인은 각각 표 평문 생성기의 병합 셀 채움,
글자층 직독 경로가 쪽별 본문을 버린 것, 해마다 공고에 딸린 같은 양식이 조금씩 다른 판으로 들어온 것(바이트
해시로는 못 잡는다), 처리 경로(doc_type)만 있고 서류 종류 축이 없던 것이었다. 여기에 비전 없이 문자 인식으로
들어가 한자 잡음만 남은 게시물 9건과, 비전 판독 결과의 표 HTML 태그가 그대로 검색 조각에 들어간 것이 더 있었다.

고친 것은 전부 일반 규칙이다(ADR-0027). 표 평문은 가로 병합을 한 번만 쓰고 새 글 없는 행은 내지 않는다.
글자층 직독은 쪽별 본문을 유지해 조각마다 쪽 번호를 붙인다. 제목 정규화 열쇠(family)로 같은 제목을 묶고 본문
겹침 0.95 이상이면 같은 내용으로 거절하며, 그 아래면 판본으로 잇는다(가장 가까운 판본 쌍이 0.90이라 오판하지
않는다). 붙임 앞머리 모양과 사업 이름 앞글자로 같은 공고의 묶음을 잡아 머리 문서에 잇는다. 서류 갈래는 제목의
낱말, 없으면 본문 생김새(조문 머리, 결재란, 빈칸 표시, 신청 기간)로 정하고 확신이 없으면 비운다. 비전 판독
결과는 표를 행 평문으로 풀어 본문에 넣는다. 깨진 게시물은 27B 비전으로 다시 읽었다(한 건에 4~5분).

전후는 이렇다. 표 조각 2,743개 중 1,444개의 평문이 바뀌어 글자 수가 811,240에서 517,902로 줄었다. 쪽 번호
없는 문서는 10건에서 2건(앞서 30쪽만 비전으로 읽은 문서 1건, 글자 없는 사진 1건)으로, 한자 잡음 문서는 9건에서
0건으로, 서류 갈래 없는 문서는 192건 전부에서 16건으로 줄었다. 판본 8건과 붙임 37건이 이어졌고 진짜 중복은
없었다. 기준 조각은 3,503개에서 3,256개로 줄었다. 검색은 같은 질의 세트(1,023건)로 다시 재서 어휘 R@1 0.423,
임베딩 0.648, 하이브리드 0.639(R@20 0.934), 운영 구성(리랭커, 표본 300) 0.707 · R@10 0.893 · MRR 0.768이다.
전날 값(0.428 · 0.644 · 0.644 · 0.723/0.907/0.782)과 견주면 전부 실행 간 흔들림(±0.013) 안이거나 그 언저리다.
조각이 줄고 본문이 깨끗해졌는데 검색 값이 그대로라는 것은, 이번 일이 검색 점수가 아니라 화면·인용·연관의
충실도를 위한 것이었다는 뜻이다. 재색인 때 한 번 기본 모델이 KURE-v1로 돌아 색인과 질의 모델이 어긋났고
서빙 점검(110)이 잡아내어 다시 만들었다. 스크립트 기본값을 운영 모델로 고쳤다.

측정 뒤 옛 비전 판독 문서 2건(422·424)의 태그 든 조각을 본문에서 다시 만들어 기준 조각은 3,270개가 됐고 태그 든
조각은 0개다. 반입 자가 점검(125)에서 같은 양식의 다른 해 판이 "같은 제목 2판(본문 겹침 91%)"으로 들어오고 같은
파일을 다시 올리면 "같은 내용의 문서가 이미 있습니다"로 거절되는 것을 실제 반입 경로로 확인했다.

이어서(9월 23일 새벽) 앞 30쪽만 읽혔던 53쪽 발전계획을 전부 비전으로 다시 읽었고(1,247초), 문자 인식으로 글이
깨진 홍보물 6건도 비전으로 다시 읽었다(두 건은 첫 시도가 시간 초과라 한 번 더). 홍보물 낱말(예매·페스타·선착순 등)을
갈래 규칙에 보태자 갈래 없는 문서는 192건 중 1건(글자 없는 사진)만 남았다. 기준 조각 3,251개로 재색인했고 서빙
점검은 통과했다.

근거: ADR-0027, data/platform/eval/retrieval-latest.json(16:22), data/logs/reprocess-20260922.log·reread-421.log·reread-posters.log, scripts/136·137.

