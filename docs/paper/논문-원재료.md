# 논문 원재료 정리

정리 2026-09-22: 장비 구성(운영 VM + 토르 02·03 서빙 + DGX 학습)과 9/20~21 학습본 채택·재반입 뒤 측정치를 본문·수치 총람에 반영하고, 9/15 시점의 "확보 대기·미측정·재측정 없음" 서술을 걷어냈다. 이전 정리 이력은 git 기록에 있다.

국문 논문 작성 시 이 문서만 보고 본문을 구성할 수 있도록 핵심만 적는다.
수치는 docs/ 아래 리포트·ADR·인수인계 문서에 있는 실측치만 쓰고, 리허설·
미확인은 그렇게 표기한다. 실험의 시계열(설정·결과·판정)은 `실험-로그.md`가
정본이며 여기서는 반복하지 않는다.

제목 후보: 온프레미스 sLLM 기반 대학 행정문서 처리 플랫폼의 설계와 도메인 특화 개선

## 1. 연구 목표

- 대학 행정문서(국고사업·채용·입학)를 교외 반출 없이 처리하는 온프레미스 AI 플랫폼 구축
- 핵심 주장 두 축
  - 축 A: 문서 반입→추출→검색→검토·작성에 이르는 전 경로의 실사용 수준 구현
  - 축 B: 오픈웨이트 모델의 도메인 특화 개선(베이스라인 대비 개선폭을 수치로 입증)
- 제약: 개인정보 문서라 외부 API 사용 불가. 장비는 운영 서버(학과 ESXi
  가상서버, CPU, GPU 없음, 상시 가동)가 웹·검색·OCR·문서관리를, 젯슨 AGX 토르
  2대(통합 메모리 122GB)가 모델 서빙을, DGX가 학습만 맡는 구성(ADR-0023).
  토르 02 = 대화·초안, 토르 03 = 반입 검토·이미지 판독 + 임베딩·리랭커 서비스.
  초기 검증(2026-09-01~07)은 DGX Spark 1대(aarch64, GB10, compute capability
  12.1, 메모리 119.7GiB — env-report)에서 했고 현재는 사용하지 않는다(HANDOFF §1)
- 시스템의 위치: 70% 초안 + 사람 검수 전제. 완성본 생성기가 아니다

## 2. 시스템 구성 (무엇을 만들었나)

- 서빙: vLLM. 초기 검증(Spark, ADR-0004)에서 nightly 이미지(다이제스트 고정) +
  Qwen3.5-35B-A3B(MoE, bf16)로 첫 토큰 87ms·30.8 tok/s를 실측했고, 같은 조건의
  dense 4B(Qwen3-4B, 47ms·23.0 tok/s)보다 빨라 대역폭 제약 장비에서 MoE가
  유리함을 확인했다. v0.11.0 릴리스는 sm_121a ptxas 미지원, FP8은 cutlass
  오류로 nightly bf16 확정. Writer 베이스는 2주차 미팅(2026-09-08)에서
  Qwen3.8-27B(dense, 멀티모달)로 확정됐고, 젯슨 토르에서 vLLM 으로 서빙한다.
  bf16 은 요청당 2.4~4.5 tok/s(동시 3요청 합계 14)라 NVFP4 판(무게 54GB→14GB,
  NVIDIA 공식)으로 바꿨다 — 같은 입력에서 10.4~10.8 tok/s, 판독·검토 3~4배 빠르고
  내용 같음(ADR-0023). 젯슨 기본 vLLM 빌드는 양자화 lm_head 를 못 받아 Qwen3.8
  전용 빌드가 필요했고 FP8 은 cuBLAS 오류로 보류. 이미지 판독·반입 검토·초안·
  대화가 모두 이 한 모델이다(계획 외 소형 모델은 제거)
- 플랫폼: FastAPI + SQLite + 서버사이드 템플릿. 업무 영역(국고사업·채용·입학·
  일반) → 프로젝트(사업·공고·학년도 단위) → 문서 접수·검토·판정·초안의 계층
  구조. 담당자·개발자 계정 분리. 운영 서버에서 systemd 자동시작, 오프라인
  모델 캐시로 자립 동작(HANDOFF §1)
- 데이터 계약: doc_chunks(제목/문단/표JSON/그림, 읽기 순서)·doc_assets(그림 파일)
  — 렌더링, CSV·MD 내보내기, 후속 검색·작성이 전부 이 계약을 읽는다(ADR-0007)
- 지식 그래프: 기준 문서·접수 문서·프로젝트를 노드로, 근거·소속·조문 참조·
  임베딩 유사를 간선으로 잇는 구조 그래프(ADR-0009, 경량 온톨로지 v1).
  문서↔문서 연관은 임베딩 유사 간선이 주 신호이고 개체 공출현·규칙 기반
  연관은 제거했다(ADR-0015). 개체(사업·부서·연도·실적) 추출은 2단계 진행 중
- 외부 참조 관문(이그레스 게이트웨이, ADR-0008): 외부 AI 질의 시 PII·기관
  식별자 세척 → 위험 판정 → 승인 큐 → 감사 기록. 실전송은 아웃바운드 개방·
  API 키 확보 전까지 비활성
- 한글(HWP) 산출: 한글 COM 에이전트 + Markdown IR(ADR-0014). 서버 지시 → 실제
  한글 화면 반영 왕복을 실장비에서 검증(2026-09-08). 초안 내보내기는
  docx·pdf·md·hwpx 4형식
- 데이터 공방(/dev/data): 검토 의견·초안·대화 기록을 학습 형식(JSONL)으로
  변환하되 출력 수치가 입력 근거에 있는 쌍만 통과. Label Studio 연동
- 규모(2026-09-22): 자동 테스트 555건 통과, ADR 24건(0001~0024), 운영 VM 문서 194건
  (규정 8·교내 113·국고 공개 68·외부 공고 5)·기준 조각 4,284개(측정 시점 4,254).
  이전 기준선(2026-09-14)은 규정 조각 1,211개였다

## 3. 방법론 — 기술별 (무엇을 어떻게 발전시켰나)

### 3.1 문서 추출 (OCR)
- 문제: 스캔·사진·손글씨·표 혼재 문서에서 기존 OCR(PaddleOCR 계열)의 오인식·구조 손실
- 방법 (입력별 경로 분기, ADR-0007):
  - 사진·4쪽 이하 PDF → 서빙 중인 VLM으로 전사. 표는 HTML(병합 셀·머리글)로,
    강조는 굵게로 지시. 문서 영역은 OpenCV 사각형 검출→원근 보정으로 자동
    크롭. 직인은 색 분리로 별도 추출. 판독 모델은 Writer(27B, 토르 03)이고
    서빙이 없으면 MinerU로 폴백. 글자층이 있어도 한자 잡음이 15%를 넘으면
    스캔본으로 보고 판독한다(chunk_quality.garbled_ratio)
  - 5쪽 이상 PDF → 디지털이면 MinerU 구조 추출 + 원본 텍스트 레이어 오버레이
    (OCR 오인식 원천 차단), 스캔이면 MinerU OCR + LLM 보수적 오타 교정(배치
    구분자 검증·길이 가드로 환각 차단)
  - 복원 뷰는 원본 렌더 배경 + 투명 텍스트 레이어. 디지털 PDF는 글자 좌표
    직독(pdfium charbox), 표는 괘선 직독
  - HWP/HWPX·텍스트는 전용 파서(.hwp 리더, ADR-0014)
- 결과 (실측, 상세는 실험-로그):
  - PaddleOCR이 깨뜨린 손글씨 표를 비전 판독이 표 구조·값까지 복원(ADR-0007)
  - 스캔 문제집 12쪽에 LLM 보수적 오타 교정 적용(ADR-0007). 교정·잔존 건수는
    미확인(원 로그 없음)
  - 장식 아이콘(120px 미만·중복) 필터, 문서당 그림 20장 상한(ADR-0007). 필터
    전후 건수는 미확인
  - MinerU 정량: 디지털 PDF 렌더를 스캔처럼 처리한 어절 F1 0.74~0.84(문서 5건,
    운영 VM CPU — ocr-cer-bench)
  - 도전자 대결: MinerU hybrid-engine·dots.ocr는 오독·환각으로 보류, PaddleOCR-VL은
    CPU에서 측정 불가(GPU 확보 후 재개)

### 3.2 검색 (RAG)
- 문제: 규정 질의에서 부분문자열 검색의 낮은 재현율
- 방법: Kiwi 명사 추출(+교내 용어 사용자 사전 26어, regulations.py) + IDF +
  희귀명사 우선의 어휘 검색과 임베딩 검색을 RRF(어휘 w_a 0.4, 임베딩 1.0)로
  융합하고, 후보 20개를 리랭커(쌍 길이 256)로 재정렬. 단계별 실측으로 채택.
  운영 모델은 학습본 ZZAIMY-Embed v2(KURE-v1 베이스)·ZZAIMY-Rerank v1
  (bge-reranker-v2-m3 베이스)이며 토르 03 서비스(:8016·:8015)로 돈다
- 초기 기준선 (합성 질의 2,595건, 조각 1,236개, 2026-09-04 — retrieval-baseline-mini):
  R@1 어휘 0.338 → 임베딩 0.611 / 하이브리드(임베딩 우세) 0.595, MRR@10 0.413 /
  0.700 / 0.692, 하이브리드 R@10 0.877(동가중 0.879). 가중 스윕은 w_a 0.2~0.5
  고원(MRR 0.692~0.693, 최적 0.3), 동가중 0.647(retrieval-weight-sweep)
- 리랭커 (표본 300건 — rerank-baseline): 하이브리드 R@1 0.490 → 0.623, R@5
  0.813 → 0.850, MRR 0.623 → 0.716. 속도는 운영 VM CPU에서 쌍 길이 256으로
  9.2s → 4.6s/질의, top-1 일치 1.00(HANDOFF §4)
- 현행 (재반입 코퍼스 4,254조각, 합성 질의 1,026건, 리랭커 표본 300, 2026-09-21,
  정본 data/platform/eval/retrieval-latest.json): R@1 어휘 0.430 · 임베딩 0.691 ·
  하이브리드 0.671 · 운영(하이브리드 + 리랭커) 0.763, 운영 R@10 0.927. 같은 표본
  에서 리랭커가 0.680 → 0.763(+0.083). 리랭커 서빙 응답 300·폴백 0
- 해석: 합성 질의 자가 검색이라 절대치는 낙관적이며 방식 간 비교용. 임베딩
  단독과 하이브리드는 R@1·MRR에서 동급이고(하이브리드는 브리프 설계의
  dense + sparse 구성), 뚜렷한 개선폭은 리랭커에서 나온다. 상황 질의(용어를
  모르고 묻는 질문)의 22.7%는 두 축 모두 상위 20 밖이라 리랭커로는 못 넘는다.
  사람 작성 골드셋(200~500문항, eval-plan) 측정 전
- 측정 규율: 측정은 반드시 서빙 장비 설정(.env.local)을 읽고 띄운다. 설정 없이
  띄우면 리랭커·질의 임베딩이 VM CPU 베이스로 조용히 물러나 수치가 거짓이
  된다(09-21 세 번 무효). 보고서는 리랭커 출처·응답·폴백 수를 항상 적는다
- 코퍼스 정제(웹 수집 노이즈 제거) 전후 비교치는 미확인 — 원 측정 기록이 남아
  있지 않다

### 3.3 도메인 특화 학습 (축 B)
- 방법: 규정 조각에서 LLM으로 합성 질의 3종(실무형·공고 요구사항형·키워드형,
  model-plan) 생성 → 문서 단위 홀드아웃 분리 → 베이스라인 측정 → 학습 → 동일
  홀드아웃 재측정. 채택은 운영 하이브리드 위에서의 개선으로만 판정
- 리허설 (embed-v0-report, 2026-09-03, CPU): 학습 쌍 2,253, 홀드아웃 96질의(편성표
  문서 7건 — 유형 편중). 베이스 R@1 0.271 / R@10 0.802 / MRR 0.429 → v0.0
  0.219 / 0.833 / 0.387. 베이스 미달로 미배포. 성과는 사이클(생성→측정→학습→
  평가→리포트) 자동 완주
- 채택 (2026-09-20, 토르 03 GPU, model-cards): ZZAIMY-Rerank v1 — 운영 하이브리드가
  실제로 올린 후보 20개로 목록 단위 학습(2에폭 21분), 문서 단위 홀드아웃 12건에서
  R@1 0.649 → 0.711·MRR 0.751 → 0.790(ADR-0020). ZZAIMY-Embed v2 — 후보 진입률로
  판정해 채택, 색인과 질의 임베딩을 같은 모델로 묶어 서빙(2에폭 20분, ADR-0021).
  v1 은 조밀 단독에서만 오르고 운영 하이브리드에서 이득이 없어 미채택 — 판정
  기준을 "운영 경로 위의 개선"으로 둔 사례. 생성 모델(27B)로 후보를 다시 매기는
  방식은 568M 리랭커보다 덜 정확하고 53배 느려 쓰지 않는다(llm-rerank-eval)
- 운영 발견: 통합메모리 장비에서 서빙(vLLM)과 CPU 학습 동시 수행 시 메모리
  스파이크로 학습이 반복 강제종료 — 서빙 일시 정지 + MALLOC_ARENA_MAX=2 +
  max_seq 512 + batch 8로 해결(dev-changelog 09-02). 스텝 시간 개선폭은 미확인.
  논문의 온프레미스 운영 절에 쓸 것
- 계획: Writer(Qwen3.8-27B)·Extract 는 교내 실물 문서 수령 후 베이스라인 측정 →
  DGX 에서 QLoRA SFT → DPO(2주차 미팅에서 부활, 선호쌍 = 담당자 판정·재작성 이력).
  학습본은 bf16 병합 → NVFP4 양자화 → 두 토르로 자동 이관(116_ship_and_serve).
  베이스라인 측정 전 파인튜닝 금지(절대 규칙)

### 3.4 개인정보 보호 설계
- 원칙: 인풋 문서는 파싱 직후 6종(주민·전화·이메일·계좌·사업자·성명, Presidio +
  한국형 인식기, 주민·사업자는 체크섬 검증) 마스킹 후에만 저장·LLM 전달. 기준
  문서(규정·공고)는 판단 근거이므로 원문 유지(ADR-0006). 마스킹은 인덱싱 이전
  단계에 배치(사후 재구축 비용 회피)
- 점검: PII 점검 화면(/dev/pii)이 마스킹 기록(유형·건수)·알려진 정답 자가
  점검·색인 본문 잔여 스캔을 제공. 외부 참조는 이그레스 게이트웨이(ADR-0008)로만

### 3.5 검토·작성 파이프라인
- 검토: 업무 영역별 프롬프트(채용은 서열화 금지·사실 정리, 입학은 기준 대조) +
  프로젝트 지침·연결 공고를 우선 근거로 주입. 판정(승인·재검토·반려) 피드백
  루프 — DPO 선호쌍의 원천
- 작성(국고사업): 공고 스키마 추출 → 섹션별 생성(통짜 생성 금지) → 수치 대조·
  배점 커버리지·예산 검산. 섹션 재료는 섹션 질의와 접수 자료 조각의 임베딩
  코사인 상위 6개(후보 40조각, ADR-0015). 담당자 의견 반영 재작성 루프. 한글
  산출은 COM 에이전트(ADR-0014)
- 수치 무결성: 생성 수치는 인출 근거에 존재해야 함(검증기) — 근거 없으면
  "(근거 없음)". 학습 데이터 정제기·데이터 공방도 같은 기준

### 3.6 연관·반영 판정의 학습된 표현 전환 (ADR-0015)
- 문제: 그래프는 개체 하나만 겹쳐도 연결(동의 절차 문서가 장학 사업계획과
  연관으로 표시), 초안 재료는 문서 앞 6조각 고정, 배점 커버리지는 키워드
  문자열 포함만 판정. 규칙·역할 사전으로 고치려는 시도는 모두 사례 짜맞추기로
  흘렀다
- 방법: 세 곳 모두 KURE-v1 임베딩의 의미 유사를 판정 근거로 통일. 커버리지는
  절대 임계값 대신 돋보임 규칙(항목별 최고 문단 코사인 − 문단 평균 ≥ 0.10,
  최고 ≥ 0.40, 문단 4개 이상). 임베딩 함수는 주입식이라 모델이 없으면 규칙
  동작으로 내려간다
- 결과 (운영 VM, KURE-v1 CPU, 2026-09-14): 그래프에서 동의 절차끼리 .862, 장학
  사업끼리 .842/.865, IACF 규정끼리 .905로 묶이고 절차↔사업은 안 엮임(개체
  코사인은 .703으로 오결합). 커버리지 절대 임계 0.62는 전부 누락으로 찍었고,
  돋보임 규칙으로 합성 표본 0/55점 → 45/55점. 크로스인코더는 시그모이드
  .500~.534로 판별 불가
- 한계: 돋보임 폭 0.10은 합성 문단 표본 기준 — 실제 초안·공고 쌍에서 재보정
  필요. 판단 품질이 임베딩 모델에 묶이므로 ZZAIMY-Embed 완성 시 세 곳이 함께
  좋아진다

## 4. 기여점 (논문 주장으로 쓸 것)

1. 외부 반출 불가 제약에서 CPU 운영 서버 + 서빙 GPU(토르 2대) + 학습 GPU(DGX)
   구성으로 추출→검색→검토·작성 전 경로를 구현하고, 실측치로 각 단계 설계
   선택을 정당화(서빙 정밀도 FP4 채택, 하이브리드 + 리랭커, 입력별 OCR 경로)
2. 서빙 중인 범용 VLM을 OCR 보완재로 쓰는 입력별 경로 분기 — 손글씨·직인 등
   전통 OCR 한계 영역을 추가 모델 없이 처리
3. 규칙·키워드 짜맞추기 대신 학습된 임베딩으로 문서 연관·초안 재료·배점 반영을
   일관되게 판정하는 설계와 실측 근거(ADR-0015) — 도메인 임베딩 파인튜닝이 세
   곳을 동시에 개선하는 구조
3-1. 검색 모델 학습본의 채택 기준을 "운영 경로 위의 개선"으로 두고 리랭커(R@1
   0.649→0.711)·임베딩(후보 진입률)을 채택한 절차(ADR-0020/0021) — 단독 지표에서
   오른 판(Embed v1)을 걸러낸 사례 포함
4. 수치 무결성 체계: 검증기·학습 데이터 정제기·데이터 공방이 같은 기준(출력
   수치 ⊂ 입력 근거)을 공유하고, 학습 사이클(생성→측정→학습→평가→리포트)을
   자동화한 재현 가능한 절차
5. 온프레미스 운영 실측 보고: 통합메모리 장비의 서빙·학습 동시 수행 한계, 젯슨
   페이지 캐시가 CUDA 메모리를 막는 함정, 양자화 판의 빌드 의존성, CPU 전용
   서버에서 VLM OCR 불가, 오프라인 모델 로딩 함정과 해법

## 5. 한계·향후 (정직하게 쓸 것)

- 검색 학습본의 홀드아웃은 문서 12~21건·상황 질문 14건 규모 — 상황 질문 줄의
  개선폭은 표본 오차 안이다. 실물 골드셋·층화 분할로 재측정
- Writer·Extract 특화 학습은 미착수 — 교내 실물 문서 수령과 베이스라인 측정이
  먼저. 지금 서빙 중인 27B 는 학습 전 베이스
- 검색 절대치는 합성 질의 자가 검색 — 골드셋 측정 전. 상황 질의의 22.7%는
  두 축 모두 상위 20 밖
- 커버리지 돋보임 폭은 합성 표본 기준 — 실제 초안으로 재보정 필요
- 5쪽 초과 스캔 PDF는 MinerU 품질 의존, 표 셀 원문 교체·셀 배경색 등 시각
  서식 미보존(ADR-0007). MinerU 의 GPU 판독 백엔드는 젯슨 빌드에서 출력이
  무너져 보류(ADR-0022)
- 공개 KURE-v2 는 다중 벡터 모델이라 현 단일 벡터 검색에 그대로 못 끼운다 —
  별도 검색 경로로 준비(ADR-0022)
- 검색은 임베딩 사전계산 방식(SQLite + npz) — 벡터DB 도입은 미결

## 6. 수치 총람 (본문 표로 옮길 것)

| 항목 | 값 | 출처 |
|---|---|---|
| 서빙 첫 토큰 / 처리량 (Qwen3.5-35B-A3B, Spark, 2026-09-01~02 — 초기 검증) | 87ms / 30.8 tok/s (dense Qwen3-4B 47ms / 23.0) | ADR-0004 |
| Writer 27B 서빙 정밀도 비교 (젯슨 토르, 2026-09-21) | bf16 2.4~4.5 tok/s → NVFP4 10.4~10.8; 판독 5쪽 128초→29초, 검토 1건 225초→82초, 내용 같음 | ADR-0023 |
| 검색 초기 기준선 R@1 / MRR@10 — 어휘 · 임베딩 · 하이브리드(임베딩 우세) | 0.338 / 0.413 · 0.611 / 0.700 · 0.595 / 0.692 | retrieval-baseline-mini (합성 질의 2,595건, 조각 1,236, 2026-09-04) |
| 검색 현행 R@1 — 어휘 · 임베딩 · 하이브리드 · 운영(+리랭커) | 0.430 · 0.691 · 0.671 · 0.763 (운영 R@10 0.927) | retrieval-latest.json (질의 1,026, 조각 4,254, 표본 300, 2026-09-21) |
| 리랭커 학습본 효과 (홀드아웃 12문서) | R@1 0.649→0.711, MRR 0.751→0.790; 질의당 3.75s→0.07s(토르 GPU) | model-cards/zzaimy-rerank-v1 · ADR-0020 |
| 하이브리드 R@10 | 0.877 (임베딩 우세) · 0.879 (동가중) | 동상 |
| 가중 스윕 최적 | w_a 0.3, MRR 0.693 (서비스 기본 0.4, MRR 0.692) | retrieval-weight-sweep |
| 리랭커 효과 (표본 300건) | R@1 0.490→0.623, R@5 0.813→0.850, MRR 0.623→0.716 | rerank-baseline |
| 리랭커 속도 (VM CPU, 쌍 길이 256) | 9.2s→4.6s/질의, top-1 일치 1.00 | HANDOFF §4 |
| Embed 리허설 (홀드아웃 96질의, 학습 쌍 2,253) | R@1 0.271→0.219 (베이스 미달), R@10 0.802→0.833 | embed-v0-report |
| OCR 어절 F1 (MinerU, VM CPU, 디지털 PDF 렌더) | 0.7427~0.8448 (5건, IACF 규정 4건 평균 ≈0.81) | ocr-cer-bench · ocr-duel |
| 배점 커버리지 의미 판정 (합성 표본) | 키워드만 0/55점 → 45/55점 | ADR-0015 (재보정 대상) |
| 그래프 연관 코사인 (KURE-v1) | 동의 절차끼리 .862, 장학 사업끼리 .842/.865, IACF 규정끼리 .905; 개체 코사인은 절차↔사업 .703(오결합) | ADR-0015 |
| 자동화 테스트 | 555건 통과 (2026-09-21) | HANDOFF |
| 결정 기록 | ADR 24건 (0001~0024) | docs/decisions |
| 저장소 규모 | 운영 VM 문서 194건 · 기준 조각 4,284 (2026-09-21 재반입; 측정 시점 4,254) | HANDOFF §4 · dev-now |
| 파일럿 코퍼스 (공개 국고 문서, 학습용) | 108건 수집, 87문서 · 1,693조각 · PII 68건 인제스트 | dev-now 2026-09-08 |
