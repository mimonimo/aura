# models/

학습 산출물이 놓이는 자리다. 가중치는 저장소에 넣지 않는다(장비에 둔다).

- 실제 가중치: 토르 `~/zzaimy/models/` (zzaimy-embed-v2 · zzaimy-rerank-v1 · 베이스 모델)
- 모델 카드(무엇을 어떤 자료로 학습했고 어떻게 쟀는지): [`docs/model-cards/`](../docs/model-cards/)
- `cards/` 는 초기 구조에서 온 빈 자리다. 카드는 위 경로로 모았다.
